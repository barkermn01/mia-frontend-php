<div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="flex items-center justify-between mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">Order Details</h1>
                <p class="text-gray-600 mt-1">Order #<?= htmlspecialchars($order['orderNumber'] ?? $order['uuid']) ?></p>
            </div>
            <a href="/orders" 
               class="bg-gray-100 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-200 transition-colors">
                <i class="fas fa-arrow-left mr-2"></i>
                Back to Orders
            </a>
        </div>

        <!-- Order Status -->
        <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between">
                <div>
                    <h2 class="text-lg font-semibold text-gray-900 mb-2">Order Status</h2>
                    <div class="flex items-center space-x-4">
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                            <?php 
                            switch($order['status']) {
                                case 'completed': echo 'bg-green-100 text-green-800'; break;
                                case 'shipped': echo 'bg-blue-100 text-blue-800'; break;
                                case 'processing': echo 'bg-yellow-100 text-yellow-800'; break;
                                case 'cancelled': echo 'bg-red-100 text-red-800'; break;
                                default: echo 'bg-gray-100 text-gray-800';
                            }
                            ?>">
                            <i class="fas fa-circle text-xs mr-1"></i>
                            <?= ucfirst(htmlspecialchars($order['status'])) ?>
                        </span>
                        
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                            <?php 
                            switch($order['paymentStatus']) {
                                case 'paid': echo 'bg-green-100 text-green-800'; break;
                                case 'pending': echo 'bg-yellow-100 text-yellow-800'; break;
                                case 'failed': echo 'bg-red-100 text-red-800'; break;
                                case 'refunded': echo 'bg-purple-100 text-purple-800'; break;
                                default: echo 'bg-gray-100 text-gray-800';
                            }
                            ?>">
                            <i class="fas fa-credit-card text-xs mr-1"></i>
                            Payment: <?= ucfirst(htmlspecialchars($order['paymentStatus'])) ?>
                        </span>
                    </div>
                </div>
                
                <div class="mt-4 md:mt-0 text-right">
                    <p class="text-sm text-gray-600">Order Date</p>
                    <p class="text-lg font-semibold text-gray-900">
                        <?php 
                        $createdAt = $order['createdAt'] ?? null;
                        if ($createdAt) {
                            if (is_numeric($createdAt)) {
                                // Unix timestamp (seconds)
                                echo date('F j, Y', $createdAt);
                            } else {
                                // ISO date string
                                $timestamp = strtotime($createdAt);
                                if ($timestamp !== false) {
                                    echo date('F j, Y', $timestamp);
                                } else {
                                    echo 'Invalid date';
                                }
                            }
                        } else {
                            echo 'N/A';
                        }
                        ?>
                    </p>
                </div>
            </div>
        </div>

        <!-- Order Items -->
        <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
            <h2 class="text-lg font-semibold text-gray-900 mb-4">Items Ordered</h2>
            <div class="space-y-4">
                <?php foreach ($order['items'] as $item): ?>
                    <div class="flex items-center space-x-4 py-4 border-b border-gray-100 last:border-b-0">
                        <div class="flex-shrink-0">
                            <?php if (!empty($item['image'])): ?>
                                <img src="<?= htmlspecialchars($item['image']) ?>" 
                                     alt="<?= htmlspecialchars($item['name'] ?? $item['sku']) ?>"
                                     class="w-20 h-20 object-cover rounded-lg">
                            <?php else: ?>
                                <div class="w-20 h-20 bg-gray-200 rounded-lg flex items-center justify-center">
                                    <i class="fas fa-image text-gray-400"></i>
                                </div>
                            <?php endif; ?>
                        </div>
                        
                        <div class="flex-1 min-w-0">
                            <h3 class="font-medium text-gray-900 truncate">
                                <?= htmlspecialchars($item['name'] ?? $item['sku']) ?>
                            </h3>
                            <p class="text-sm text-gray-600">
                                SKU: <?= htmlspecialchars($item['sku']) ?>
                            </p>
                            <p class="text-sm text-gray-600">
                                Quantity: <?= $item['qty'] ?>
                            </p>
                        </div>
                        
                        <div class="text-right">
                            <p class="font-medium text-gray-900">
                                <?php 
                                $currency = $order['currency'] ?? 'GBP';
                                $symbols = ['GBP' => '£', 'USD' => '$', 'EUR' => '€'];
                                $symbol = $symbols[$currency] ?? $currency;
                                echo $symbol . number_format($item['price'] / 100, 2);
                                ?>
                            </p>
                            <p class="text-sm text-gray-600">
                                Total: <?= $symbol . number_format(($item['price'] * $item['qty']) / 100, 2) ?>
                            </p>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
            
            <!-- Order Total -->
            <div class="mt-6 pt-6 border-t border-gray-200">
                <div class="flex justify-between text-lg font-bold">
                    <span>Order Total</span>
                    <span class="text-blue-600"><?= $symbol . number_format($order['total'] / 100, 2) ?></span>
                </div>
            </div>
        </div>

        <!-- Shipping Information -->
        <?php if (!empty($order['shippingAddress'])): ?>
            <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                <h2 class="text-lg font-semibold text-gray-900 mb-4">Shipping Information</h2>
                <div class="text-gray-700">
                    <?php if (!empty($order['shippingAddress']['line1'])): ?>
                        <p><?= htmlspecialchars($order['shippingAddress']['line1']) ?></p>
                    <?php endif; ?>
                    <?php if (!empty($order['shippingAddress']['line2'])): ?>
                        <p><?= htmlspecialchars($order['shippingAddress']['line2']) ?></p>
                    <?php endif; ?>
                    <p>
                        <?= htmlspecialchars($order['shippingAddress']['city'] ?? '') ?>
                        <?php if (!empty($order['shippingAddress']['state'])): ?>
                            , <?= htmlspecialchars($order['shippingAddress']['state']) ?>
                        <?php endif; ?>
                        <?= htmlspecialchars($order['shippingAddress']['postalCode'] ?? '') ?>
                    </p>
                    <p><?= htmlspecialchars($order['shippingAddress']['country'] ?? '') ?></p>
                </div>
                
                <?php if (!empty($order['trackingNumber'])): ?>
                    <div class="mt-4 pt-4 border-t border-gray-200">
                        <p class="text-sm text-gray-600 mb-1">Tracking Number</p>
                        <p class="font-mono text-gray-900"><?= htmlspecialchars($order['trackingNumber']) ?></p>
                    </div>
                <?php endif; ?>
            </div>
        <?php endif; ?>

        <!-- Payment Information -->
        <?php if (!empty($order['paymentEvents']) || !empty($order['refunds'])): ?>
            <div class="bg-white rounded-lg shadow-lg p-6">
                <h2 class="text-lg font-semibold text-gray-900 mb-4">Payment History</h2>
                
                <?php if (!empty($order['paymentEvents'])): ?>
                    <div class="space-y-2 mb-4">
                        <?php foreach ($order['paymentEvents'] as $event): ?>
                            <div class="flex items-center justify-between py-2 border-b border-gray-100 last:border-b-0">
                                <div>
                                    <p class="font-medium text-gray-900"><?= htmlspecialchars($event['type']) ?></p>
                                    <p class="text-sm text-gray-600">
                                        <?php 
                                        $processedAt = $event['processedAt'] ?? null;
                                        if ($processedAt) {
                                            if (is_numeric($processedAt)) {
                                                echo date('F j, Y g:i A', $processedAt);
                                            } else {
                                                $timestamp = strtotime($processedAt);
                                                echo $timestamp !== false ? date('F j, Y g:i A', $timestamp) : 'Invalid date';
                                            }
                                        } else {
                                            echo 'N/A';
                                        }
                                        ?>
                                    </p>
                                </div>
                                <span class="text-green-600">
                                    <i class="fas fa-check-circle"></i>
                                </span>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
                
                <?php if (!empty($order['refunds'])): ?>
                    <div class="mt-4 pt-4 border-t border-gray-200">
                        <h3 class="font-medium text-gray-900 mb-3">Refunds</h3>
                        <div class="space-y-2">
                            <?php foreach ($order['refunds'] as $refund): ?>
                                <div class="flex items-center justify-between py-2 bg-purple-50 rounded-lg px-3">
                                    <div>
                                        <p class="font-medium text-gray-900">
                                            <?= $symbol . number_format($refund['amount'] / 100, 2) ?>
                                        </p>
                                        <p class="text-sm text-gray-600">
                                            <?= htmlspecialchars($refund['reason']) ?>
                                        </p>
                                        <p class="text-xs text-gray-500">
                                            <?php 
                                            $refundDate = $refund['createdAt'] ?? null;
                                            if ($refundDate) {
                                                if (is_numeric($refundDate)) {
                                                    echo date('F j, Y', $refundDate);
                                                } else {
                                                    $timestamp = strtotime($refundDate);
                                                    echo $timestamp !== false ? date('F j, Y', $timestamp) : 'Invalid date';
                                                }
                                            } else {
                                                echo 'N/A';
                                            }
                                            ?>
                                        </p>
                                    </div>
                                    <span class="text-purple-600">
                                        <i class="fas fa-undo"></i>
                                    </span>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </div>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</div>
