<?php
$status = $_GET['status'] ?? null;
$orderId = $_GET['orderId'] ?? null;
$cartId = $_GET['cartId'] ?? null;
?>

<div class="container mx-auto px-4 py-8">
    <div class="max-w-3xl mx-auto">
        <?php if ($status === 'success' && isset($order)): ?>
            <!-- Success State -->
            <div class="bg-white rounded-lg shadow-lg p-8">
                <div class="text-center mb-8">
                    <div class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4">
                        <i class="fas fa-check text-3xl text-green-600"></i>
                    </div>
                    <h1 class="text-3xl font-bold text-gray-800 mb-2">Payment Successful!</h1>
                    <p class="text-gray-600">Thank you for your order. We've received your payment.</p>
                </div>

                <!-- Order Details -->
                <div class="border-t border-b border-gray-200 py-6 mb-6">
                    <div class="grid grid-cols-2 gap-4 mb-4">
                        <div>
                            <p class="text-sm text-gray-600">Order Number</p>
                            <p class="font-semibold text-gray-900"><?= htmlspecialchars($order['orderNumber'] ?? $order['uuid']) ?></p>
                        </div>
                        <div>
                            <p class="text-sm text-gray-600">Order Date</p>
                            <p class="font-semibold text-gray-900">
                                <?php 
                                $createdAt = $order['createdAt'];
                                if (is_numeric($createdAt)) {
                                    echo date('F j, Y', (int)$createdAt);
                                } elseif (is_string($createdAt)) {
                                    echo date('F j, Y', strtotime($createdAt));
                                } else {
                                    echo 'N/A';
                                }
                                ?>
                            </p>
                        </div>
                        <div>
                            <p class="text-sm text-gray-600">Payment Status</p>
                            <p class="font-semibold text-green-600">
                                <i class="fas fa-check-circle mr-1"></i>
                                <?= ucfirst(htmlspecialchars($order['paymentStatus'])) ?>
                            </p>
                        </div>
                        <div>
                            <p class="text-sm text-gray-600">Total Amount</p>
                            <p class="font-semibold text-gray-900">
                                <?php 
                                $currency = $order['currency'] ?? 'GBP';
                                $symbols = ['GBP' => '£', 'USD' => '$', 'EUR' => '€'];
                                $symbol = $symbols[$currency] ?? $currency;
                                echo $symbol . number_format($order['total'] / 100, 2);
                                ?>
                            </p>
                        </div>
                    </div>

                    <?php if (!empty($order['customerEmail'])): ?>
                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                            <p class="text-sm text-blue-800">
                                <i class="fas fa-envelope mr-2"></i>
                                A confirmation email has been sent to <strong><?= htmlspecialchars($order['customerEmail']) ?></strong>
                            </p>
                        </div>
                    <?php endif; ?>
                </div>

                <!-- Order Items -->
                <?php if (!empty($order['items'])): ?>
                    <div class="mb-6">
                        <h2 class="text-lg font-semibold text-gray-900 mb-4">Order Items</h2>
                        <div class="space-y-3">
                            <?php foreach ($order['items'] as $item): ?>
                                <div class="flex items-center space-x-4 py-3 border-b border-gray-100 last:border-b-0">
                                    <div class="flex-shrink-0">
                                        <?php if (!empty($item['image'])): ?>
                                            <img src="<?= htmlspecialchars($item['image']) ?>" 
                                                 alt="<?= htmlspecialchars($item['productTitle'] ?? $item['title'] ?? $item['name'] ?? $item['sku']) ?>"
                                                 class="w-16 h-16 object-cover rounded-lg">
                                        <?php else: ?>
                                            <div class="w-16 h-16 bg-gray-200 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-image text-gray-400"></i>
                                            </div>
                                        <?php endif; ?>
                                    </div>
                                    
                                    <div class="flex-1 min-w-0">
                                        <h3 class="font-medium text-gray-900 truncate">
                                            <?= htmlspecialchars($item['productTitle'] ?? $item['title'] ?? $item['name'] ?? $item['sku']) ?>
                                        </h3>
                                        <p class="text-sm text-gray-600">
                                            Quantity: <?= $item['qty'] ?>
                                        </p>
                                    </div>
                                    
                                    <div class="text-right">
                                        <p class="font-medium text-gray-900">
                                            <?= $symbol . number_format($item['price'] / 100, 2) ?>
                                        </p>
                                        <p class="text-sm text-gray-600">
                                            Total: <?= $symbol . number_format(($item['price'] * $item['qty']) / 100, 2) ?>
                                        </p>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </div>
                <?php endif; ?>

                <!-- Shipping Information -->
                <?php if (!empty($order['shippingAddress'])): ?>
                    <div class="mb-6">
                        <h2 class="text-lg font-semibold text-gray-900 mb-4">Shipping Address</h2>
                        <div class="bg-gray-50 rounded-lg p-4">
                            <?php if (!empty($order['shippingAddress']['name'])): ?>
                                <p class="font-medium text-gray-900"><?= htmlspecialchars($order['shippingAddress']['name']) ?></p>
                            <?php endif; ?>
                            <?php if (!empty($order['shippingAddress']['line1'])): ?>
                                <p class="text-gray-700"><?= htmlspecialchars($order['shippingAddress']['line1']) ?></p>
                            <?php endif; ?>
                            <?php if (!empty($order['shippingAddress']['line2'])): ?>
                                <p class="text-gray-700"><?= htmlspecialchars($order['shippingAddress']['line2']) ?></p>
                            <?php endif; ?>
                            <p class="text-gray-700">
                                <?= htmlspecialchars($order['shippingAddress']['city'] ?? '') ?>
                                <?php if (!empty($order['shippingAddress']['state'])): ?>
                                    , <?= htmlspecialchars($order['shippingAddress']['state']) ?>
                                <?php endif; ?>
                                <?= htmlspecialchars($order['shippingAddress']['postalCode'] ?? '') ?>
                            </p>
                            <p class="text-gray-700"><?= htmlspecialchars($order['shippingAddress']['country'] ?? '') ?></p>
                            <?php if (!empty($order['shippingAddress']['phone'])): ?>
                                <p class="text-gray-700 mt-2">
                                    <i class="fas fa-phone text-gray-500 mr-1"></i>
                                    <?= htmlspecialchars($order['shippingAddress']['phone']) ?>
                                </p>
                            <?php endif; ?>
                        </div>
                    </div>
                <?php endif; ?>

                <!-- Actions -->
                <div class="flex flex-col sm:flex-row gap-3 justify-center">
                    <?php if ($isLoggedIn ?? false): ?>
                        <a href="/orders/<?= htmlspecialchars($order['uuid']) ?>" 
                           class="bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors text-center">
                            <i class="fas fa-receipt mr-2"></i>
                            View Order Details
                        </a>
                    <?php endif; ?>
                    <a href="/products" 
                       class="bg-gray-100 text-gray-700 px-6 py-3 rounded-lg font-medium hover:bg-gray-200 transition-colors text-center">
                        <i class="fas fa-shopping-bag mr-2"></i>
                        Continue Shopping
                    </a>
                </div>
            </div>

        <?php elseif ($status === 'cancelled'): ?>
            <!-- Cancelled State -->
            <div class="bg-white rounded-lg shadow-lg p-8 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-yellow-100 rounded-full mb-4">
                    <i class="fas fa-times text-3xl text-yellow-600"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Payment Cancelled</h1>
                <p class="text-gray-600 mb-6">Your payment was cancelled. Your cart is still available.</p>
                
                <div class="flex flex-col sm:flex-row gap-3 justify-center">
                    <a href="/cart" 
                       class="bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors">
                        <i class="fas fa-shopping-cart mr-2"></i>
                        Return to Cart
                    </a>
                    <a href="/products" 
                       class="bg-gray-100 text-gray-700 px-6 py-3 rounded-lg font-medium hover:bg-gray-200 transition-colors">
                        <i class="fas fa-shopping-bag mr-2"></i>
                        Continue Shopping
                    </a>
                </div>
            </div>

        <?php else: ?>
            <!-- Error State -->
            <div class="bg-white rounded-lg shadow-lg p-8 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-red-100 rounded-full mb-4">
                    <i class="fas fa-exclamation-triangle text-3xl text-red-600"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Something Went Wrong</h1>
                <p class="text-gray-600 mb-6">
                    <?= isset($error) ? htmlspecialchars($error) : 'We encountered an error processing your checkout.' ?>
                </p>
                
                <div class="flex flex-col sm:flex-row gap-3 justify-center">
                    <a href="/cart" 
                       class="bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors">
                        <i class="fas fa-shopping-cart mr-2"></i>
                        Return to Cart
                    </a>
                    <a href="/contact" 
                       class="bg-gray-100 text-gray-700 px-6 py-3 rounded-lg font-medium hover:bg-gray-200 transition-colors">
                        <i class="fas fa-envelope mr-2"></i>
                        Contact Support
                    </a>
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>
