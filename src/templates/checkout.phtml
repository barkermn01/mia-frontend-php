<div class="container mx-auto px-4 py-8">
    <div class="max-w-6xl mx-auto">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">Checkout</h1>

        <?php if (!empty($cart['items'])): ?>
            <?php if (isset($error)): ?>
                <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6">
                    <div class="flex">
                        <i class="fas fa-exclamation-circle mr-2 mt-0.5"></i>
                        <span><?= htmlspecialchars($error) ?></span>
                    </div>
                </div>
            <?php endif; ?>
            
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                <!-- Checkout Information -->
                <div class="space-y-6">
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-semibold mb-4">Secure Checkout</h2>
                        
                        <div class="space-y-4">
                            <div class="flex items-start">
                                <i class="fas fa-shield-alt text-blue-600 text-xl mr-3 mt-1"></i>
                                <div>
                                    <h3 class="font-medium text-gray-900 mb-1">Secure Payment Processing</h3>
                                    <p class="text-sm text-gray-600">
                                        Your payment will be processed securely through Stripe. All payment information is encrypted and handled by Stripe's PCI-compliant infrastructure.
                                    </p>
                                </div>
                            </div>
                            
                            <div class="flex items-start">
                                <i class="fas fa-credit-card text-blue-600 text-xl mr-3 mt-1"></i>
                                <div>
                                    <h3 class="font-medium text-gray-900 mb-1">Multiple Payment Methods</h3>
                                    <p class="text-sm text-gray-600">
                                        Stripe supports credit cards, debit cards, and other payment methods. Choose your preferred option on the next page.
                                    </p>
                                </div>
                            </div>
                            
                            <div class="flex items-start">
                                <i class="fas fa-truck text-blue-600 text-xl mr-3 mt-1"></i>
                                <div>
                                    <h3 class="font-medium text-gray-900 mb-1">Shipping & Contact Information</h3>
                                    <p class="text-sm text-gray-600">
                                        You'll provide your shipping address and contact details on the secure Stripe checkout page.
                                    </p>
                                </div>
                            </div>
                        </div>
                        
                        <div class="mt-6 bg-gray-50 border border-gray-200 rounded-lg p-4">
                            <p class="text-sm text-gray-700">
                                <i class="fas fa-info-circle text-gray-500 mr-2"></i>
                                Clicking "Proceed to Payment" will redirect you to Stripe's secure checkout page where you can complete your purchase.
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Order Summary -->
                <div class="lg:sticky lg:top-24 lg:self-start">
                    <div class="bg-white rounded-lg shadow-lg p-6">
                        <h2 class="text-xl font-semibold mb-4">Order Summary</h2>
                        
                        <!-- Items -->
                        <div class="space-y-4 mb-6">
                            <?php foreach ($cart['items'] as $item): ?>
                                <div class="flex items-center space-x-3">
                                    <div class="flex-shrink-0">
                                        <?php if (!empty($item['image'])): ?>
                                            <img src="<?= htmlspecialchars($item['image']) ?>" 
                                                 alt="<?= htmlspecialchars($item['title'] ?? $item['sku']) ?>"
                                                 class="w-12 h-12 object-cover rounded">
                                        <?php else: ?>
                                            <div class="w-12 h-12 bg-gray-200 rounded flex items-center justify-center">
                                                <i class="fas fa-image text-gray-400 text-xs"></i>
                                            </div>
                                        <?php endif; ?>
                                    </div>
                                    
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900 truncate">
                                            <?= htmlspecialchars($item['title'] ?? $item['sku']) ?>
                                        </p>
                                        <p class="text-xs text-gray-600">Qty: <?= $item['qty'] ?></p>
                                    </div>
                                    
                                    <p class="text-sm font-medium text-gray-900">
                                        <?php 
                                        $itemPrice = $view->getDisplayPrice($item['price']);
                                        $totalPrice = $itemPrice * $item['qty'];
                                        $currency = $view->getCurrency($item['price']);
                                        $symbols = ['GBP' => '£', 'USD' => '$', 'EUR' => '€'];
                                        $symbol = $symbols[$currency] ?? $currency;
                                        echo $symbol . number_format($totalPrice, 2);
                                        ?>
                                    </p>
                                </div>
                            <?php endforeach; ?>
                        </div>
                        
                        <?php 
                        // Calculate totals from cart items since API doesn't provide them
                        $subtotal = 0;
                        foreach ($cart['items'] as $item) {
                            $subtotal += ($item['price'] * $item['qty']);
                        }
                        $tax = 0; // No tax calculation for now
                        $total = $subtotal + $tax;
                        ?>
                        
                        <!-- Totals -->
                        <div class="space-y-2 border-t border-gray-200 pt-4">
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600">Subtotal</span>
                                <span class="font-medium">£<?= number_format($subtotal / 100, 2) ?></span>
                            </div>
                            
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600">Shipping</span>
                                <span class="font-medium text-green-600">Free</span>
                            </div>
                            
                            <div class="flex justify-between text-sm">
                                <span class="text-gray-600">Tax</span>
                                <span class="font-medium">£<?= number_format($tax / 100, 2) ?></span>
                            </div>
                            
                            <div class="flex justify-between text-lg font-bold border-t border-gray-200 pt-2">
                                <span>Total</span>
                                <span class="text-blue-600">£<?= number_format($total / 100, 2) ?></span>
                            </div>
                        </div>
                        
                        <!-- Place Order Button -->
                        <form method="POST" action="/checkout">
                            <button type="submit" 
                                    class="w-full bg-blue-600 text-white py-4 px-6 rounded-lg font-medium hover:bg-blue-700 transition-colors mt-6">
                                <i class="fas fa-lock mr-2"></i>
                                Proceed to Payment
                            </button>
                        </form>
                        
                        <!-- Security Notice -->
                        <div class="mt-4 flex items-center justify-center text-xs text-gray-500">
                            <i class="fas fa-shield-alt mr-1"></i>
                            Secure payment powered by Stripe
                        </div>
                    </div>
                </div>
            </div>
            
        <?php else: ?>
            <!-- Empty Cart -->
            <div class="text-center py-16">
                <div class="bg-white rounded-lg shadow-lg p-12 max-w-md mx-auto">
                    <i class="fas fa-shopping-cart text-6xl text-gray-400 mb-6"></i>
                    <h2 class="text-2xl font-bold text-gray-800 mb-4">Your cart is empty</h2>
                    <p class="text-gray-600 mb-6">Add some products before checking out.</p>
                    <a href="/products" 
                       class="bg-blue-600 text-white px-8 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors inline-block">
                        <i class="fas fa-shopping-bag mr-2"></i>
                        Browse Products
                    </a>
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>