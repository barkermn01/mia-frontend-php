<div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-bold mb-8">Contact Us</h1>

        <!-- Contact Methods -->
        <div class="grid md:grid-cols-2 gap-6 mb-12">
            <!-- Phone -->
            <div class="bg-white rounded-lg shadow-lg p-6">
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 rounded-lg flex items-center justify-center" style="background-color: var(--ox-red-light);">
                            <i class="fas fa-phone-alt text-xl" style="color: var(--ox-red);"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="text-xl font-semibold mb-2">Phone</h3>
                        <p class="text-gray-700 mb-1">
                            <a href="tel:<?= htmlspecialchars($view->setting('CONTACT:phone_number', '')) ?>" class="hover:underline" style="color: var(--ox-red);"><?= htmlspecialchars($view->setting('CONTACT:phone_display', '+44 (0)1234 567890')) ?></a>
                        </p>
                        <p class="text-sm text-gray-600"><?= htmlspecialchars($view->setting('CONTACT:phone_hours', 'Monday - Friday: 9am - 5pm')) ?></p>
                    </div>
                </div>
            </div>

            <!-- Email -->
            <div class="bg-white rounded-lg shadow-lg p-6">
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 rounded-lg flex items-center justify-center" style="background-color: var(--ox-red-light);">
                            <i class="fas fa-envelope text-xl" style="color: var(--ox-red);"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="text-xl font-semibold mb-2">Email</h3>
                        <p class="text-gray-700 mb-1">
                            <a href="mailto:<?= htmlspecialchars($view->setting('CONTACT:support_email', 'support@example.com')) ?>" class="hover:underline" style="color: var(--ox-red);"><?= htmlspecialchars($view->setting('CONTACT:support_email', 'support@example.com')) ?></a>
                        </p>
                        <p class="text-sm text-gray-600">We typically respond within 24 hours</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Addresses -->
        <div class="grid md:grid-cols-2 gap-6 mb-12">
            <!-- Registered Office -->
            <div class="bg-white rounded-lg shadow-lg p-6">
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 rounded-lg flex items-center justify-center" style="background-color: var(--ox-red-light);">
                            <i class="fas fa-building text-xl" style="color: var(--ox-red);"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="text-xl font-semibold mb-2">Registered Office</h3>
                        <p class="text-gray-700">
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_name', 'Company Name')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_line1', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_line2', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_line3', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_postcode', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:registered_office_country', '')) ?>
                        </p>
                        <p class="text-sm text-gray-600 mt-3">
                            Company Registration: <?= htmlspecialchars($view->setting('BRAND:company_registration', 'N/A')) ?><br>
                            VAT Number: <?= htmlspecialchars($view->setting('BRAND:vat_number', 'N/A')) ?>
                        </p>
                    </div>
                </div>
            </div>

            <!-- Warehouse -->
            <div class="bg-white rounded-lg shadow-lg p-6">
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 rounded-lg flex items-center justify-center" style="background-color: var(--ox-red-light);">
                            <i class="fas fa-warehouse text-xl" style="color: var(--ox-red);"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="text-xl font-semibold mb-2">Warehouse & Returns</h3>
                        <p class="text-gray-700">
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_name', 'Warehouse')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_line1', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_line2', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_line3', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_postcode', '')) ?><br>
                            <?= htmlspecialchars($view->setting('CONTACT:warehouse_country', '')) ?>
                        </p>
                        <p class="text-sm text-gray-600 mt-3">
                            Please contact us before returning any items
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- FAQ Link -->
        <div class="bg-gradient-to-r from-blue-600 to-blue-700 rounded-lg shadow-lg p-8 text-white text-center">
            <h2 class="text-2xl font-semibold mb-4">Need Quick Answers?</h2>
            <p class="text-lg mb-6">
                Check out our Help Center for answers to frequently asked questions about orders, delivery, returns, and more.
            </p>
            <a href="/help" class="inline-block bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition-colors">
                Visit Help Center
            </a>
        </div>
    </div>
</div>
