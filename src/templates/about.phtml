<div class="container mx-auto px-4 py-8">
    <?php
    // Get about page content from settings
    $aboutContent = $view->setting('CONTENT:about_page');
    
    if ($aboutContent):
        // Render markdown content with proper styling
    ?>
    <div class="markdown-content">
        <?= \Marti\Frontend\MarkdownRenderer::render($aboutContent) ?>
    </div>
    <?php else: ?>
        <!-- Fallback content if setting not found -->
    <!-- Hero Section -->
    <div class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4">About <?= htmlspecialchars($view->setting('BRAND:company_name', 'Us')) ?></h1>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            Your trusted partner for premium products
        </p>
    </div>

    <!-- Main Content -->
    <div class="max-w-4xl mx-auto space-y-12">
        <!-- Company Story -->
        <section class="bg-white rounded-lg shadow-lg p-8">
            <h2 class="text-3xl font-semibold mb-6">Our Story</h2>
            <div class="prose prose-lg max-w-none text-gray-700 space-y-4">
                <p>
                    We are committed to providing quality products and excellent service to our customers.
                </p>
            </div>
        </section>
    </div>
    <?php endif; ?>
</div>
