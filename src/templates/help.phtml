<div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
        <?php
        // Get help page content from settings
        $helpContent = $view->setting('CONTENT:help_page');
        
        if ($helpContent):
            // Render markdown content with proper styling
        ?>
        <div class="markdown-content">
            <?= \Marti\Frontend\MarkdownRenderer::render($helpContent) ?>
        </div>
        <?php else: ?>
            <!-- Fallback content if setting not found -->
        <h1 class="text-4xl font-bold mb-8">Help Center</h1>

        <!-- Contact Information -->
        <section class="bg-white rounded-lg shadow-lg p-8 mb-8">
            <h2 class="text-2xl font-semibold mb-6">Contact Us</h2>
            <div class="grid md:grid-cols-2 gap-6">
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                            <i class="fas fa-envelope text-blue-600 text-xl"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="font-semibold mb-1">Email</h3>
                        <p class="text-gray-600"><?= htmlspecialchars($view->setting('CONTACT:support_email', 'support@example.com')) ?></p>
                        <p class="text-sm text-gray-500 mt-1">We typically respond within 24 hours</p>
                    </div>
                </div>

                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                            <i class="fas fa-phone text-blue-600 text-xl"></i>
                        </div>
                    </div>
                    <div>
                        <h3 class="font-semibold mb-1">Phone</h3>
                        <p class="text-gray-600"><?= htmlspecialchars($view->setting('CONTACT:phone_display', '+44 (0)1234 567890')) ?></p>
                        <p class="text-sm text-gray-500 mt-1"><?= htmlspecialchars($view->setting('CONTACT:phone_hours', 'Mon-Fri: 9am-5pm')) ?></p>
                    </div>
                </div>
            </div>
        </section>
        <?php endif; ?>
    </div>
</div>
