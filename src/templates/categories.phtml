<div class="container mx-auto px-4 py-8">
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-2">All Categories</h1>
        <p class="text-gray-600">Browse products by category</p>
    </div>

    <?php if (!empty($categories)): ?>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <?php foreach ($categories as $category): ?>
                <?php 
                $categoryName = $category['name'];
                $categorySlug = str_replace(' ', '-', $categoryName);
                $categoryUrl = '/category/' . rawurlencode($categorySlug);
                
                // Get metadata for this category (passed from controller)
                $metadata = $categoryMetadata[$categoryName] ?? [
                    'image' => '/images/logo-small.png',
                    'tagline' => $category['count'] . ' ' . ($category['count'] === 1 ? 'product' : 'products'),
                    'display_text' => $categoryName
                ];
                ?>
                <a href="<?= $categoryUrl ?>" class="bg-white rounded-lg shadow-lg hover:shadow-xl transition-shadow overflow-hidden group">
                    <div class="relative h-48 bg-gray-200 overflow-hidden">
                        <img src="<?= htmlspecialchars($metadata['image']) ?>" 
                             alt="<?= htmlspecialchars($metadata['display_text']) ?>" 
                             class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
                             onerror="this.src='/images/logo-small.png'">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
                        <div class="absolute bottom-0 left-0 right-0 p-4">
                            <h2 class="text-xl font-semibold text-white mb-1">
                                <?= htmlspecialchars($metadata['display_text']) ?>
                            </h2>
                        </div>
                    </div>
                    <div class="p-6">
                        <p class="text-gray-600 mb-2">
                            <?= htmlspecialchars($metadata['tagline']) ?>
                        </p>
                        <span class="text-red-600 font-medium group-hover:underline">
                            View Products <i class="fas fa-arrow-right ml-1"></i>
                        </span>
                    </div>
                </a>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <div class="text-center py-16">
            <div class="bg-white rounded-lg shadow-lg p-12 max-w-md mx-auto">
                <i class="fas fa-tags text-6xl text-gray-400 mb-6"></i>
                <h2 class="text-2xl font-bold text-gray-800 mb-4">No Categories Found</h2>
                <p class="text-gray-600 mb-6">There are no product categories available at the moment.</p>
                <a href="/" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block">
                    Back to Home
                </a>
            </div>
        </div>
    <?php endif; ?>
</div>
