<div class="container mx-auto px-4 py-8 max-w-4xl">
    <?php
    // Get privacy page content from settings
    $privacyContent = $view->setting('CONTENT:privacy_page');
    
    if ($privacyContent):
        // Render markdown content with proper styling
    ?>
    <div class="markdown-content">
        <?= \Marti\Frontend\MarkdownRenderer::render($privacyContent) ?>
    </div>
    <?php else: ?>
        <!-- Fallback content if setting not found -->
    <h1 class="text-4xl font-bold mb-8">Privacy Policy</h1>
    
    <div class="prose prose-lg max-w-none space-y-6">
        <section>
            <h2 class="text-2xl font-semibold mb-4">1. Introduction</h2>
            <p class="text-gray-700 mb-4">
                Welcome to <?= htmlspecialchars($view->setting('BRAND:company_name', 'our website')) ?>. We are committed to protecting your privacy and ensuring the security of your personal information.
            </p>
        </section>

        <section>
            <h2 class="text-2xl font-semibold mb-4">12. Contact Us</h2>
            <p class="text-gray-700 mb-4">
                If you have any questions about this Privacy Policy or our privacy practices, please contact us at:
            </p>
            <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
                <p class="text-gray-700">
                    <strong><?= htmlspecialchars($view->setting('BRAND:company_name', 'Company Name')) ?></strong><br>
                    A trading name of <?= htmlspecialchars($view->setting('BRAND:company_legal_name', 'Legal Name')) ?><br>
                    Company Registration: <?= htmlspecialchars($view->setting('BRAND:company_registration', 'N/A')) ?><br>
                    VAT Number: <?= htmlspecialchars($view->setting('BRAND:vat_number', 'N/A')) ?><br><br>
                    Email: <?= htmlspecialchars($view->setting('CONTACT:privacy_email', 'privacy@example.com')) ?><br>
                    Phone: <?= htmlspecialchars($view->setting('CONTACT:phone_display', '+44 (0)1234 567890')) ?><br>
                    Address: <?= htmlspecialchars($view->setting('CONTACT:registered_office_line1', '')) ?>, <?= htmlspecialchars($view->setting('CONTACT:registered_office_line2', '')) ?>, <?= htmlspecialchars($view->setting('CONTACT:registered_office_line3', '')) ?>, <?= htmlspecialchars($view->setting('CONTACT:registered_office_postcode', '')) ?>, <?= htmlspecialchars($view->setting('CONTACT:registered_office_country', '')) ?>
                </p>
            </div>
        </section>

        <div class="mt-8 pt-8 border-t border-gray-300">
            <p class="text-sm text-gray-600">
                <strong>Last Updated:</strong> <?= date('F j, Y') ?>
            </p>
        </div>
    </div>
    <?php endif; ?>
</div>
