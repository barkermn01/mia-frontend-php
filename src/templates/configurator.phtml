<section class="container mx-auto px-4 py-8">
    <!-- Breadcrumb -->
    <nav class="mb-6" id="cfg-breadcrumb">
        <ol class="flex items-center space-x-2 text-sm text-gray-600 flex-wrap">
            <li><a href="/" class="hover:text-red-600">Home</a></li>
            <li><i class="fas fa-chevron-right text-xs"></i></li>
            <li class="text-gray-900">Configurator</li>
        </ol>
    </nav>

    <h1 class="text-3xl font-bold text-gray-800 mb-8">
        <i class="fas fa-cogs mr-2" style="color: var(--brand-primary);"></i>
        Exhaust System Configurator
    </h1>

    <!-- Vehicle Selection -->
    <div id="configurator-page" class="bg-white rounded-lg shadow-lg p-6 mb-8">
        <h2 class="text-xl font-semibold mb-4">Select Your Vehicle</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Make</label>
                <select data-cfg="make" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-red-500 focus:border-red-500">
                    <option value="">-- Select Make --</option>
                    <?php foreach ($makes as $m): ?>
                        <option value="<?= htmlspecialchars($m) ?>" <?= $m === $selectedMake ? 'selected' : '' ?>><?= htmlspecialchars($m) ?></option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Model</label>
                <select data-cfg="model" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-red-500 focus:border-red-500"
                        <?= empty($models) ? 'disabled' : '' ?>>
                    <option value="">-- Select Model --</option>
                    <?php foreach ($models as $mod): ?>
                        <option value="<?= htmlspecialchars($mod) ?>" <?= $mod === $selectedModel ? 'selected' : '' ?>><?= htmlspecialchars($mod) ?></option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Variant</label>
                <select data-cfg="variant" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-red-500 focus:border-red-500"
                        <?= empty($variants) ? 'disabled' : '' ?>>
                    <option value="">-- Select Variant --</option>
                    <?php foreach ($variants as $desc => $info): ?>
                        <option value="<?= htmlspecialchars($desc) ?>" <?= $desc === $selectedVariant ? 'selected' : '' ?>>
                            <?= htmlspecialchars($desc) ?> (<?= htmlspecialchars($info['year_range']) ?>)
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
    </div>

    <!-- Results rendered by JS -->
    <div data-cfg="results" id="cfg-results">
        <?php if ($selectedVariant && !empty($systems)): ?>
            <!-- Server-rendered initial results (will be replaced by JS on interaction) -->
            <div class="mb-4">
                <h2 class="text-2xl font-semibold text-gray-800 mb-2">
                    <?= htmlspecialchars($selectedMake . ' ' . $selectedModel) ?>
                    <span class="text-gray-500 text-lg">— <?= htmlspecialchars($selectedVariant) ?></span>
                </h2>
                <?php if ($variantInfo): ?>
                    <p class="text-gray-600"><?= htmlspecialchars($variantInfo['year_range'] ?? '') ?> · <?= count($systems) ?> system<?= count($systems) !== 1 ? 's' : '' ?> available</p>
                <?php endif; ?>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <?php
                usort($systems, function($a, $b) {
                    $aMs = !empty($a['motorsport']) ? 0 : 1;
                    $bMs = !empty($b['motorsport']) ? 0 : 1;
                    return $aMs - $bMs;
                });
                ?>
                <?php foreach ($systems as $sys): ?>
                    <a href="/systems/<?= rawurlencode($selectedMake) ?>/<?= rawurlencode($selectedModel) ?>/<?= $view->slug($selectedVariant) ?>/<?= $view->slug($sys['system_name'] ?? '') ?>/<?= rawurlencode($sys['system_number']) ?>"
                       class="block bg-white rounded-lg shadow-lg hover:shadow-xl transition-shadow border-2 border-transparent hover:border-red-500 overflow-hidden">
                        <div class="h-48 bg-gray-100 flex items-center justify-center overflow-hidden relative">
                            <?php if (!empty($sys['motorsport'])): ?>
                                <span class="absolute top-2 left-2 bg-red-600 text-white px-2 py-1 rounded text-xs font-bold z-10"><i class="fas fa-flag-checkered mr-1"></i>Motorsport</span>
                            <?php endif; ?>
                            <?php if (!empty($sys['system_detail_images'])): ?>
                                <?php $imgSrc = $sys['system_detail_images'][0]; ?>
                                <img src="<?= htmlspecialchars(str_starts_with($imgSrc, 'http') ? $imgSrc : '/images/systems/' . $imgSrc) ?>"
                                     alt="<?= htmlspecialchars($sys['system_number']) ?>"
                                     class="w-full h-full object-contain"
                                     onerror="this.style.display='none'; this.nextElementSibling.style.display='block'">
                                <i class="fas fa-cog text-5xl text-gray-300" style="display:none"></i>
                            <?php else: ?>
                                <i class="fas fa-cog text-5xl text-gray-300"></i>
                            <?php endif; ?>
                        </div>
                        <div class="p-4">
                            <h3 class="text-lg font-bold text-gray-800 mb-1"><?= htmlspecialchars($sys['system_number']) ?></h3>
                            <?php if (!empty($sys['pipe_diameter'])): ?>
                                <p class="text-sm text-gray-600 mb-1"><i class="fas fa-ruler mr-1"></i> <?= htmlspecialchars($sys['pipe_diameter']) ?></p>
                            <?php endif; ?>
                            <p class="text-sm text-gray-500"><?= count($sys['part_numbers']) ?> part<?= count($sys['part_numbers']) !== 1 ? 's' : '' ?></p>
                            <?php if (!empty($sys['fitting_kit_pdf'])): ?>
                                <p class="text-xs text-blue-600 mt-1"><i class="fas fa-file-pdf mr-1"></i> Fitting kit available</p>
                            <?php endif; ?>
                        </div>
                    </a>
                <?php endforeach; ?>
            </div>
        <?php elseif (!$selectedMake): ?>
            <div class="bg-white rounded-lg shadow-lg p-12 text-center">
                <i class="fas fa-car text-6xl text-gray-300 mb-4"></i>
                <h3 class="text-xl font-semibold text-gray-700 mb-2">Find Your Exhaust System</h3>
                <p class="text-gray-500">Select your vehicle make above to get started.</p>
            </div>
        <?php endif; ?>
    </div>
</section>

<script src="/js/configurator.js"></script>
<script>
Configurator.init('configurator-page');
Configurator.resultsContainer = document.getElementById('cfg-results');
</script>
