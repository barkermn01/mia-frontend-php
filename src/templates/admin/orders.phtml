<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-gray-900">Orders Management</h3>
        <div class="flex space-x-2">
            <button class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition-colors">
                <i class="fas fa-download mr-2"></i>Export
            </button>
        </div>
    </div>

    <!-- Search Filters -->
    <div class="bg-white rounded-lg shadow p-4">
        <form method="GET" action="/admin/orders" class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-4">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                <select name="status" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="">All Statuses</option>
                    <option value="pending" <?= ($filters['status'] ?? '') === 'pending' ? 'selected' : '' ?>>Pending</option>
                    <option value="paid" <?= ($filters['status'] ?? '') === 'paid' ? 'selected' : '' ?>>Paid</option>
                    <option value="processing" <?= ($filters['status'] ?? '') === 'processing' ? 'selected' : '' ?>>Processing</option>
                    <option value="shipped" <?= ($filters['status'] ?? '') === 'shipped' ? 'selected' : '' ?>>Shipped</option>
                    <option value="completed" <?= ($filters['status'] ?? '') === 'completed' ? 'selected' : '' ?>>Completed</option>
                    <option value="cancelled" <?= ($filters['status'] ?? '') === 'cancelled' ? 'selected' : '' ?>>Cancelled</option>
                    <option value="refund-requested" <?= ($filters['status'] ?? '') === 'refund-requested' ? 'selected' : '' ?>>Refund Requested</option>
                    <option value="refunded" <?= ($filters['status'] ?? '') === 'refunded' ? 'selected' : '' ?>>Refunded</option>
                    <option value="failed" <?= ($filters['status'] ?? '') === 'failed' ? 'selected' : '' ?>>Failed</option>
                </select>
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Customer Email</label>
                <input type="text" name="customerEmail" value="<?= htmlspecialchars($filters['customerEmail'] ?? '') ?>" 
                       placeholder="Search email..." 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Item Name</label>
                <input type="text" name="itemName" value="<?= htmlspecialchars($filters['itemName'] ?? '') ?>" 
                       placeholder="Search item..." 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Shipping City</label>
                <input type="text" name="shippingCity" value="<?= htmlspecialchars($filters['shippingCity'] ?? '') ?>" 
                       placeholder="Search city..." 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Postal Code</label>
                <input type="text" name="shippingPostalCode" value="<?= htmlspecialchars($filters['shippingPostalCode'] ?? '') ?>" 
                       placeholder="Search postcode..." 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Country</label>
                <input type="text" name="shippingCountry" value="<?= htmlspecialchars($filters['shippingCountry'] ?? '') ?>" 
                       placeholder="e.g., GB, US..." 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            
            <div class="flex items-end space-x-2">
                <button type="submit" class="flex-1 bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 transition-colors">
                    <i class="fas fa-search mr-2"></i>Search
                </button>
                <a href="/admin/orders" class="bg-gray-300 text-gray-700 px-4 py-2 rounded-md hover:bg-gray-400 transition-colors">
                    <i class="fas fa-times"></i>
                </a>
            </div>
        </form>
    </div>

    <!-- Orders Table -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <?php if (!empty($orders)): ?>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Order ID</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Customer</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Order Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Payment Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <?php foreach ($orders as $order): ?>
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                    <a href="/admin/orders/<?= htmlspecialchars($order['uuid'] ?? $order['id']) ?>" 
                                       class="text-blue-600 hover:text-blue-900 hover:underline">
                                        <?= htmlspecialchars($order['orderNumber'] ?? $order['uuid'] ?? $order['id'] ?? 'N/A') ?>
                                    </a>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="text-sm text-gray-900">
                                        <?php if (!empty($order['customerName'])): ?>
                                            <?= htmlspecialchars($order['customerName']) ?>
                                        <?php elseif (!empty($order['customerId']) && $order['customerId'] !== 'guest'): ?>
                                            <span class="text-blue-600">Registered User</span>
                                        <?php else: ?>
                                            <span class="text-gray-500">Guest</span>
                                        <?php endif; ?>
                                    </div>
                                    <div class="text-sm text-gray-500">
                                        <?= htmlspecialchars($order['customerEmail'] ?? $order['customer']['email'] ?? '') ?>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                    <?php if (!empty($order['createdAt'])): ?>
                                        <?php 
                                        $createdAt = $order['createdAt'];
                                        if (is_array($createdAt) && isset($createdAt['$date'])) {
                                            // MongoDB date format
                                            if (is_array($createdAt['$date']) && isset($createdAt['$date']['$numberLong'])) {
                                                $timestamp = (int)(intval($createdAt['$date']['$numberLong']) / 1000);
                                            } elseif (is_numeric($createdAt['$date'])) {
                                                $timestamp = (int)(intval($createdAt['$date']) / 1000);
                                            } else {
                                                $timestamp = 0;
                                            }
                                        } elseif (is_numeric($createdAt)) {
                                            // Plain Unix timestamp
                                            $timestamp = (int)$createdAt;
                                        } else {
                                            // String date
                                            $timestamp = strtotime($createdAt);
                                        }
                                        echo date('M j, Y', $timestamp);
                                        ?>
                                    <?php else: ?>
                                        <span class="text-gray-400">Unknown</span>
                                    <?php endif; ?>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                    <?php if (!empty($order['total'])): ?>
                                        £<?= number_format($order['total'] / 100, 2) ?>
                                    <?php else: ?>
                                        <span class="text-gray-400">N/A</span>
                                    <?php endif; ?>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <?php
                                    $status = $order['status'] ?? 'pending';
                                    $statusColors = [
                                        'pending' => 'bg-yellow-100 text-yellow-800',
                                        'paid' => 'bg-cyan-100 text-cyan-800',
                                        'processing' => 'bg-blue-100 text-blue-800',
                                        'shipped' => 'bg-purple-100 text-purple-800',
                                        'completed' => 'bg-green-100 text-green-800',
                                        'cancelled' => 'bg-red-100 text-red-800',
                                        'refunded' => 'bg-orange-100 text-orange-800',
                                        'refund-requested' => 'bg-yellow-100 text-yellow-800',
                                        'failed' => 'bg-red-100 text-red-800'
                                    ];
                                    $colorClass = $statusColors[$status] ?? 'bg-gray-100 text-gray-800';
                                    ?>
                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full <?= $colorClass ?>">
                                        <?= htmlspecialchars(ucfirst($status)) ?>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <?php
                                    $paymentStatus = $order['paymentStatus'] ?? 'pending';
                                    $paymentStatusColors = [
                                        'pending' => 'bg-yellow-100 text-yellow-800',
                                        'paid' => 'bg-green-100 text-green-800',
                                        'partially_refunded' => 'bg-orange-100 text-orange-800',
                                        'refunded' => 'bg-red-100 text-red-800',
                                        'failed' => 'bg-red-100 text-red-800',
                                        'cancelled' => 'bg-gray-100 text-gray-800'
                                    ];
                                    $paymentColorClass = $paymentStatusColors[$paymentStatus] ?? 'bg-gray-100 text-gray-800';
                                    
                                    // Format display text
                                    $paymentStatusDisplay = str_replace('_', ' ', ucfirst($paymentStatus));
                                    ?>
                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full <?= $paymentColorClass ?>">
                                        <?= htmlspecialchars($paymentStatusDisplay) ?>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                    <div class="flex space-x-3">
                                        <a href="/admin/orders/<?= htmlspecialchars($order['uuid'] ?? $order['id']) ?>" 
                                           class="text-blue-600 hover:text-blue-900 transition-colors" 
                                           title="View Order">
                                            <i class="fas fa-eye fa-lg"></i>
                                        </a>
                                        
                                        <?php
                                        $status = $order['status'] ?? 'pending';
                                        $orderId = htmlspecialchars($order['uuid'] ?? $order['id']);
                                        ?>
                                        
                                        <?php if ($status === 'paid'): ?>
                                            <!-- Paid orders can be processed or cancelled -->
                                            <button class="process-order-btn text-green-600 hover:text-green-900 transition-colors" 
                                                    title="Mark as Processing"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-cog fa-lg"></i>
                                            </button>
                                            <button class="cancel-order-btn text-red-600 hover:text-red-900 transition-colors" 
                                                    title="Cancel Order"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-times fa-lg"></i>
                                            </button>
                                        <?php elseif ($status === 'processing'): ?>
                                            <!-- Processing orders can be shipped or cancelled -->
                                            <button class="ship-order-btn text-purple-600 hover:text-purple-900 transition-colors" 
                                                    title="Mark as Shipped"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-shipping-fast fa-lg"></i>
                                            </button>
                                            <button class="cancel-order-btn text-red-600 hover:text-red-900 transition-colors" 
                                                    title="Cancel Order"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-times fa-lg"></i>
                                            </button>
                                        <?php elseif ($status === 'shipped'): ?>
                                            <!-- Shipped orders can be marked as completed -->
                                            <button class="complete-order-btn text-green-600 hover:text-green-900 transition-colors" 
                                                    title="Mark as Completed"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-check-circle fa-lg"></i>
                                            </button>
                                        <?php elseif ($status === 'pending'): ?>
                                            <!-- Pending orders can only be cancelled -->
                                            <button class="cancel-order-btn text-red-600 hover:text-red-900 transition-colors" 
                                                    title="Cancel Order"
                                                    data-order-id="<?= $orderId ?>">
                                                <i class="fas fa-times fa-lg"></i>
                                            </button>
                                        <?php elseif (in_array($status, ['completed', 'cancelled', 'refunded'])): ?>
                                            <!-- Final states - no actions available -->
                                            <span class="text-gray-400 text-xs">No actions</span>
                                        <?php endif; ?>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <?php if ($total > 20): ?>
                <div class="bg-white px-4 py-3 border-t border-gray-200 sm:px-6">
                    <div class="flex items-center justify-between">
                        <div class="text-sm text-gray-700">
                            Showing <?= (($page - 1) * 20) + 1 ?> to <?= min($page * 20, $total) ?> of <?= $total ?> results
                        </div>
                        <div class="flex space-x-2">
                            <?php if ($page > 1): ?>
                                <a href="?page=<?= $page - 1 ?>" 
                                   class="px-3 py-2 text-sm bg-gray-300 text-gray-700 rounded hover:bg-gray-400">
                                    Previous
                                </a>
                            <?php endif; ?>
                            <?php if ($page * 20 < $total): ?>
                                <a href="?page=<?= $page + 1 ?>" 
                                   class="px-3 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700">
                                    Next
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
        <?php else: ?>
            <div class="text-center py-12">
                <i class="fas fa-shopping-cart text-4xl text-gray-400 mb-4"></i>
                <h3 class="text-lg font-medium text-gray-900 mb-2">No orders found</h3>
                <p class="text-gray-500">Orders will appear here once customers start making purchases.</p>
            </div>
        <?php endif; ?>
    </div>
</div>

<script src="/js/admin/orders.js"></script>