<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-gray-900">System Settings</h3>
    </div>

    <!-- API Configuration -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-lg font-medium text-gray-900 mb-4">API Configuration</h4>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">API URL</label>
                <input type="text" value="<?= htmlspecialchars($config['api_url']) ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Site ID</label>
                <input type="text" value="<?= htmlspecialchars($config['site_id']) ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">SSL Verification</label>
                <input type="text" value="<?= $config['verify_ssl'] ? 'Enabled' : 'Disabled' ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Debug Mode</label>
                <input type="text" value="<?= $config['debug'] ? 'Enabled' : 'Disabled' ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
        </div>
        <div class="mt-4 p-4 bg-blue-50 rounded-lg">
            <p class="text-sm text-blue-800">
                <i class="fas fa-info-circle mr-2"></i>
                API settings are configured via environment variables in the .env file.
            </p>
        </div>
    </div>

    <!-- Cache Configuration -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-lg font-medium text-gray-900 mb-4">Cache Configuration</h4>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Cache Status</label>
                <input type="text" value="<?= $config['cache_enabled'] ? 'Enabled' : 'Disabled' ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Memcached Host</label>
                <input type="text" value="<?= htmlspecialchars($config['memcached_host']) ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Cache TTL</label>
                <input type="text" value="<?= $config['cache_ttl'] ?> seconds" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
        </div>
    </div>

    <!-- Admin Configuration -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-lg font-medium text-gray-900 mb-4">Admin Panel Configuration</h4>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Admin Path</label>
                <input type="text" value="<?= htmlspecialchars($config['admin_address']) ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Admin URL</label>
                <input type="text" value="<?= htmlspecialchars($_SERVER['HTTP_HOST'] . '/' . $config['admin_address']) ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
        </div>
    </div>

    <!-- System Information -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-lg font-medium text-gray-900 mb-4">System Information</h4>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">PHP Version</label>
                <input type="text" value="<?= PHP_VERSION ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Server Software</label>
                <input type="text" value="<?= $_SERVER['SERVER_SOFTWARE'] ?? 'Unknown' ?>" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50" readonly>
            </div>
        </div>
    </div>

    <!-- Actions -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-lg font-medium text-gray-900 mb-4">System Actions</h4>
        <div class="flex space-x-4">
            <button class="bg-yellow-600 text-white px-4 py-2 rounded-lg hover:bg-yellow-700 transition-colors">
                <i class="fas fa-sync mr-2"></i>Clear Cache
            </button>
            <button class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                <i class="fas fa-download mr-2"></i>Export Settings
            </button>
        </div>
    </div>
</div>