<div class="space-y-6">
    <!-- Welcome Section -->
    <div class="bg-white rounded-lg shadow p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-2">
            Welcome back<?php if (!empty($user['firstName'])): ?>, <?= htmlspecialchars($user['firstName']) ?><?php endif; ?>!
        </h3>
        <p class="text-gray-600">
            You are logged in as <?= htmlspecialchars(ucfirst(str_replace('_', ' ', $user['role']))) ?>
            <?php if (!empty($user['email'])): ?>
                (<?= htmlspecialchars($user['email']) ?>)
            <?php endif; ?>
        </p>
    </div>

    <!-- Quick Actions -->
    <div class="bg-white rounded-lg shadow p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Quick Actions</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <a href="<?= $adminPath ?>/products" class="flex items-center p-4 bg-blue-50 rounded-lg hover:bg-blue-100 transition-colors">
                <i class="fas fa-plus-circle text-blue-600 text-xl mr-3"></i>
                <div>
                    <div class="font-medium text-gray-900">Manage Products</div>
                    <div class="text-sm text-gray-600">Add, edit, or remove products</div>
                </div>
            </a>
            
            <a href="<?= $adminPath ?>/orders" class="flex items-center p-4 bg-green-50 rounded-lg hover:bg-green-100 transition-colors">
                <i class="fas fa-list-alt text-green-600 text-xl mr-3"></i>
                <div>
                    <div class="font-medium text-gray-900">View Orders</div>
                    <div class="text-sm text-gray-600">Process and manage orders</div>
                </div>
            </a>
            
            <a href="<?= $adminPath ?>/settings" class="flex items-center p-4 bg-purple-50 rounded-lg hover:bg-purple-100 transition-colors">
                <i class="fas fa-cog text-purple-600 text-xl mr-3"></i>
                <div>
                    <div class="font-medium text-gray-900">Settings</div>
                    <div class="text-sm text-gray-600">Configure system settings</div>
                </div>
            </a>
        </div>
    </div>
</div>