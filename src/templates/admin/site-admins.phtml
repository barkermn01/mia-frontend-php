<div class="space-y-6">
    <!-- Success Message -->
    <?php if (isset($_GET['success'])): ?>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-check-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($_GET['success']) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Header -->
    <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-gray-900">Site Administrators</h3>
        <a href="<?= $adminPath ?>/site-admins/add" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
            <i class="fas fa-plus mr-2"></i>Add Site Admin
        </a>
    </div>

    <!-- Search and Filters -->
    <div class="bg-white rounded-lg shadow p-6">
        <form method="GET" class="flex items-center space-x-4">
            <div class="flex-1">
                <input type="text" name="search" value="<?= htmlspecialchars($search) ?>" 
                       placeholder="Search by name or email..." 
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <button type="submit" class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 transition-colors">
                <i class="fas fa-search mr-2"></i>Search
            </button>
            <?php if ($search): ?>
                <a href="?" class="bg-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-400 transition-colors">
                    <i class="fas fa-times mr-2"></i>Clear
                </a>
            <?php endif; ?>
        </form>
    </div>

    <!-- Admins Table -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <?php if (!empty($admins)): ?>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Admin</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Role</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Created</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <?php foreach ($admins as $admin): ?>
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 h-10 w-10">
                                            <div class="h-10 w-10 bg-blue-600 rounded-full flex items-center justify-center">
                                                <i class="fas fa-user-shield text-white"></i>
                                            </div>
                                        </div>
                                        <div class="ml-4">
                                            <div class="text-sm font-medium text-gray-900">
                                                <?php 
                                                $displayName = '';
                                                if (!empty($admin['name'])) {
                                                    $displayName = $admin['name'];
                                                } elseif (!empty($admin['firstName']) || !empty($admin['lastName'])) {
                                                    $displayName = trim(($admin['firstName'] ?? '') . ' ' . ($admin['lastName'] ?? ''));
                                                } else {
                                                    $displayName = 'N/A';
                                                }
                                                echo htmlspecialchars($displayName);
                                                ?>
                                            </div>
                                            <div class="text-sm text-gray-500">
                                                <?= htmlspecialchars($admin['email']) ?>
                                            </div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full 
                                        <?= ($admin['role'] ?? '') === 'super_admin' ? 'bg-purple-100 text-purple-800' : 'bg-blue-100 text-blue-800' ?>">
                                        <?= htmlspecialchars(ucfirst(str_replace('_', ' ', $admin['role'] ?? 'site_admin'))) ?>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full 
                                        <?= ($admin['status'] ?? 'active') === 'active' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' ?>">
                                        <?= htmlspecialchars(ucfirst($admin['status'] ?? 'active')) ?>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                    <?php if (isset($admin['createdAt'])): ?>
                                        <?php 
                                        $createdAt = $admin['createdAt'];
                                        if (is_array($createdAt) && isset($createdAt['$date'])) {
                                            // MongoDB date format
                                            if (isset($createdAt['$date']['$numberLong'])) {
                                                $timestamp = (int)($createdAt['$date']['$numberLong'] / 1000);
                                                echo date('M j, Y', $timestamp);
                                            } elseif (is_string($createdAt['$date'])) {
                                                echo date('M j, Y', strtotime($createdAt['$date']));
                                            } else {
                                                echo 'N/A';
                                            }
                                        } elseif (is_string($createdAt)) {
                                            echo date('M j, Y', strtotime($createdAt));
                                        } else {
                                            echo 'N/A';
                                        }
                                        ?>
                                    <?php else: ?>
                                        N/A
                                    <?php endif; ?>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                    <div class="flex space-x-3">
                                        <a href="<?= $adminPath ?>/site-admins/edit?id=<?= urlencode($admin['id'] ?? $admin['uuid'] ?? '') ?>" 
                                           class="text-blue-600 hover:text-blue-900 transition-colors" title="Edit Admin">
                                            <i class="fas fa-edit fa-lg"></i>
                                        </a>
                                        <?php if (($admin['id'] ?? $admin['uuid'] ?? '') !== ($_SESSION['customer']['id'] ?? $_SESSION['customer']['uuid'] ?? '')): ?>
                                            <button class="delete-admin-btn text-red-600 hover:text-red-900 transition-colors" 
                                                    title="Delete Admin" 
                                                    data-admin-id="<?= htmlspecialchars($admin['id'] ?? $admin['uuid'] ?? '') ?>"
                                                    data-admin-name="<?= htmlspecialchars($admin['name'] ?? $admin['email']) ?>">
                                                <i class="fas fa-trash fa-lg"></i>
                                            </button>
                                        <?php else: ?>
                                            <span class="text-gray-400" title="Cannot delete yourself">
                                                <i class="fas fa-trash fa-lg"></i>
                                            </span>
                                        <?php endif; ?>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <?php if ($total > 20): ?>
                <div class="bg-white px-4 py-3 border-t border-gray-200 sm:px-6">
                    <div class="flex items-center justify-between">
                        <div class="text-sm text-gray-700">
                            Showing <?= (($page - 1) * 20) + 1 ?> to <?= min($page * 20, $total) ?> of <?= $total ?> results
                        </div>
                        <div class="flex space-x-2">
                            <?php if ($page > 1): ?>
                                <a href="?page=<?= $page - 1 ?><?= $search ? '&search=' . urlencode($search) : '' ?>" 
                                   class="px-3 py-2 text-sm bg-gray-300 text-gray-700 rounded hover:bg-gray-400">
                                    Previous
                                </a>
                            <?php endif; ?>
                            <?php if ($page * 20 < $total): ?>
                                <a href="?page=<?= $page + 1 ?><?= $search ? '&search=' . urlencode($search) : '' ?>" 
                                   class="px-3 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700">
                                    Next
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
        <?php else: ?>
            <div class="text-center py-12">
                <i class="fas fa-user-shield text-4xl text-gray-400 mb-4"></i>
                <h3 class="text-lg font-medium text-gray-900 mb-2">No site admins found</h3>
                <p class="text-gray-500 mb-4">
                    <?= $search ? 'No admins match your search criteria.' : 'Get started by adding your first site admin.' ?>
                </p>
                <a href="<?= $adminPath ?>/site-admins/add" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                    <i class="fas fa-plus mr-2"></i>Add Site Admin
                </a>
            </div>
        <?php endif; ?>
    </div>
</div>

<script src="/js/ModalManager.js"></script>
<script src="/js/admin/site-admins.js"></script>
<script>
// Initialize the site admins management with configuration data
document.addEventListener('DOMContentLoaded', function() {
    window.AdminSiteAdminsConfig = {
        adminPath: '<?= $adminPath ?>'
    };
});
</script>