<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= \Marti\Frontend\HtmlResources::getInstance()->getTitle() ?></title>
    
    <?= \Marti\Frontend\HtmlResources::getInstance()->renderMeta() ?>
    <?= \Marti\Frontend\HtmlResources::getInstance()->renderFavicons() ?>
    <?= \Marti\Frontend\HtmlResources::getInstance()->renderCss() ?>
    <?= \Marti\Frontend\HtmlResources::getInstance()->renderJsHead() ?>
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Toast Notifications -->
    <div id="toast-container"></div>

    <div class="flex h-screen">
        <!-- Sidebar -->
        <div class="bg-gray-800 text-white w-64 flex-shrink-0">
            <div class="p-4">
                <h1 class="text-xl font-bold">
                    <i class="fas fa-cog mr-2"></i>Admin Panel
                </h1>
                <p class="text-gray-400 text-sm mt-1">OxWinches</p>
            </div>
            
            <nav class="mt-8">
                <a href="<?= $adminPath ?>/" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-tachometer-alt mr-3"></i>
                    Dashboard
                </a>
                <a href="<?= $adminPath ?>/products" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-box mr-3"></i>
                    Products
                </a>
                <a href="<?= $adminPath ?>/stock" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-warehouse mr-3"></i>
                    Stock Management
                </a>
                <a href="<?= $adminPath ?>/shipping" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-shipping-fast mr-3"></i>
                    Shipping
                </a>
                <a href="<?= $adminPath ?>/orders" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-shopping-cart mr-3"></i>
                    Orders
                </a>
                <a href="<?= $adminPath ?>/alerts" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none relative">
                    <i class="fas fa-bell mr-3"></i>
                    Alerts
                    <?php if (isset($alertCount) && $alertCount > 0): ?>
                        <span class="ml-auto bg-red-600 text-white text-xs font-bold px-2 py-1 rounded-full">
                            <?= $alertCount ?>
                        </span>
                    <?php endif; ?>
                </a>
                <a href="<?= $adminPath ?>/customers" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-users mr-3"></i>
                    Customers
                </a>
                <a href="<?= $adminPath ?>/site-admins" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-user-shield mr-3"></i>
                    Site Admins
                </a>
                <a href="<?= $adminPath ?>/settings" class="flex items-center px-4 py-3 text-gray-300 hover:bg-gray-700 hover:text-white transition-colors focus:outline-none">
                    <i class="fas fa-cog mr-3"></i>
                    Settings
                </a>
            </nav>
            
            <div class="absolute bottom-0 w-64 p-4 border-t border-gray-700">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <div class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center">
                            <i class="fas fa-user text-white text-sm"></i>
                        </div>
                    </div>
                    <div class="ml-3">
                        <p class="text-sm font-medium"><?= htmlspecialchars($user['email']) ?></p>
                        <p class="text-xs text-gray-400"><?= htmlspecialchars(ucfirst(str_replace('_', ' ', $user['role']))) ?></p>
                    </div>
                </div>
                <a href="/logout" class="mt-3 block text-center bg-red-600 text-white py-2 px-4 rounded hover:bg-red-700 transition-colors text-sm">
                    <i class="fas fa-sign-out-alt mr-2"></i>Logout
                </a>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- Header -->
            <header class="bg-white shadow-sm border-b border-gray-200">
                <div class="px-6 py-4">
                    <div class="flex items-center justify-between">
                        <h2 class="text-2xl font-semibold text-gray-800"><?= $title ?? 'Admin Panel' ?></h2>
                        <div class="flex items-center space-x-4">
                            <a href="/" class="text-blue-600 hover:text-blue-800 transition-colors">
                                <i class="fas fa-external-link-alt mr-1"></i>
                                View Store
                            </a>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Main Content Area -->
            <main class="flex-1 overflow-y-auto p-6">
                <?= $content ?>
            </main>
        </div>
    </div>

    <!-- Scripts -->
    <script>
        // Configuration for the JavaScript app
        window.MiaStoreConfig = {
            isAdmin: true,
            adminPath: '<?= $adminPath ?>',
            vatRate: <?= $vatRate ?? 20 ?>,
            productFormData: <?= isset($productFormData) ? json_encode($productFormData) : 'null' ?>
        };
    </script>
    <script src="/js/ModalManager.js"></script>
    <?= \Marti\Frontend\HtmlResources::getInstance()->renderJsBody() ?>
</body>
</html>