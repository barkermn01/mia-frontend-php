<div class="max-w-4xl mx-auto space-y-6">
    <!-- Error Message -->
    <?php if (isset($error)): ?>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-exclamation-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($error) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Header -->
    <div>
        <h3 class="text-lg font-semibold text-gray-900"><?= $isEdit ? 'Edit' : 'Add' ?> Shipping Method</h3>
        <p class="text-sm text-gray-600 mt-1">Configure shipping method details and regional pricing</p>
    </div>

    <form method="POST" action="<?= $adminPath ?>/shipping/<?= $isEdit ? 'edit' : 'add' ?>" id="shipping-form" class="space-y-6">
        <?php if ($isEdit && isset($method['id'])): ?>
            <input type="hidden" name="method_id" value="<?= htmlspecialchars($method['id']) ?>">
        <?php endif; ?>

        <!-- Basic Information -->
        <div class="bg-white rounded-lg shadow p-6 space-y-4">
            <h4 class="text-md font-medium text-gray-900 mb-4">Basic Information</h4>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">
                    Method Name <span class="text-red-500">*</span>
                </label>
                <input type="text" name="name" required
                       value="<?= htmlspecialchars($method['name'] ?? '') ?>"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       placeholder="e.g., Royal Mail Standard">
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Provider</label>
                <input type="text" name="provider"
                       value="<?= htmlspecialchars($method['provider'] ?? '') ?>"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       placeholder="e.g., Royal Mail, DHL, FedEx">
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Delivery Time</label>
                <input type="text" name="delivery_time"
                       value="<?= htmlspecialchars($method['deliveryTime'] ?? '') ?>"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       placeholder="e.g., 3-5 business days">
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Maximum Weight (kg)</label>
                <input type="number" name="max_weight" step="0.01" min="0"
                       value="<?= htmlspecialchars($method['maxWeightKg'] ?? '') ?>"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       placeholder="e.g., 30.00">
                <p class="text-xs text-gray-500 mt-1">Leave empty for no weight limit</p>
            </div>

            <div class="flex items-center">
                <input type="checkbox" name="enabled" value="1" id="enabled"
                       <?= (!isset($method['enabled']) || $method['enabled']) ? 'checked' : '' ?>
                       class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded">
                <label for="enabled" class="ml-2 block text-sm text-gray-900">
                    Enabled (visible to customers)
                </label>
            </div>
        </div>

        <!-- Regions -->
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between mb-4">
                <div>
                    <h4 class="text-md font-medium text-gray-900">Regional Pricing</h4>
                    <p class="text-sm text-gray-600">Configure pricing for different countries</p>
                </div>
                <button type="button" id="add-region-btn" class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition-colors">
                    <i class="fas fa-plus mr-2"></i>Add Region
                </button>
            </div>

            <div id="regions-container" class="space-y-4">
                <!-- Regions will be added here by JavaScript -->
            </div>

            <input type="hidden" id="regions-data" name="regions" value="">
        </div>

        <!-- Actions -->
        <div class="flex items-center justify-between">
            <a href="<?= $adminPath ?>/shipping" class="text-gray-600 hover:text-gray-900">
                <i class="fas fa-arrow-left mr-2"></i>Back to Shipping Methods
            </a>
            <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                <i class="fas fa-save mr-2"></i><?= $isEdit ? 'Update' : 'Create' ?> Shipping Method
            </button>
        </div>
    </form>
</div>

<script>
// Pass initial data to JavaScript
window.ShippingFormData = {
    adminPath: '<?= $adminPath ?>',
    isEdit: <?= $isEdit ? 'true' : 'false' ?>,
    regions: <?= json_encode($method['regions'] ?? []) ?>
};
</script>
<script src="/js/ModalManager.js"></script>
<script src="/js/admin/shipping-form.js"></script>
<script>
window.modalManager = new ModalManager();
</script>
