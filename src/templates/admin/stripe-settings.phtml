<div class="space-y-6">
    <!-- Success Message -->
    <?php if (isset($_GET['success'])): ?>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-check-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($_GET['success']) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Error Message -->
    <?php if (isset($_GET['error'])): ?>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-exclamation-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($_GET['error']) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Header -->
    <div>
        <h3 class="text-lg font-semibold text-gray-900">Stripe Configuration</h3>
        <p class="text-sm text-gray-600 mt-1">Configure Stripe payment processing for your store</p>
    </div>

    <!-- Configuration Form -->
    <div class="bg-white rounded-lg shadow p-6">
        <form method="POST" action="<?= $adminPath ?>/settings" class="space-y-6">
            <!-- Stripe Secret Key -->
            <div>
                <label for="stripe_secret_key" class="block text-sm font-medium text-gray-700 mb-2">
                    Stripe Secret Key
                    <span class="text-red-500">*</span>
                </label>
                <input type="password" 
                       id="stripe_secret_key" 
                       name="setting_value" 
                       required
                       placeholder="sk_test_... or sk_live_..."
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <input type="hidden" name="setting_name" value="stripe_secret_key">
                <input type="hidden" name="setting_type" value="secret">
                <p class="text-xs text-gray-500 mt-1">
                    <i class="fas fa-info-circle mr-1"></i>
                    Use <code>sk_test_</code> for testing, <code>sk_live_</code> for production. This value is encrypted and cannot be viewed after saving.
                </p>
            </div>

            <button type="submit" 
                    class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                <i class="fas fa-save mr-2"></i>
                Save Secret Key
            </button>
        </form>

        <div class="border-t border-gray-200 my-6"></div>

        <form method="POST" action="<?= $adminPath ?>/settings" class="space-y-6">
            <!-- Stripe Publishable Key -->
            <div>
                <label for="stripe_publishable_key" class="block text-sm font-medium text-gray-700 mb-2">
                    Stripe Publishable Key
                    <span class="text-red-500">*</span>
                </label>
                <input type="text" 
                       id="stripe_publishable_key" 
                       name="setting_value" 
                       required
                       placeholder="pk_test_... or pk_live_..."
                       value="<?= htmlspecialchars($settings['stripe_publishable_key']['value'] ?? '') ?>"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <input type="hidden" name="setting_name" value="stripe_publishable_key">
                <input type="hidden" name="setting_type" value="text">
                <p class="text-xs text-gray-500 mt-1">
                    <i class="fas fa-info-circle mr-1"></i>
                    This key is safe to use in your frontend code
                </p>
            </div>

            <button type="submit" 
                    class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                <i class="fas fa-save mr-2"></i>
                Save Publishable Key
            </button>
        </form>

        <div class="border-t border-gray-200 my-6"></div>

        <form method="POST" action="<?= $adminPath ?>/settings" class="space-y-6">
            <!-- Stripe Webhook Secret -->
            <div>
                <label for="stripe_webhook_secret" class="block text-sm font-medium text-gray-700 mb-2">
                    Stripe Webhook Secret
                    <span class="text-red-500">*</span>
                </label>
                <input type="password" 
                       id="stripe_webhook_secret" 
                       name="setting_value" 
                       required
                       placeholder="whsec_..."
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <input type="hidden" name="setting_name" value="stripe_webhook_secret">
                <input type="hidden" name="setting_type" value="secret">
                <p class="text-xs text-gray-500 mt-1">
                    <i class="fas fa-info-circle mr-1"></i>
                    Get this from Stripe Dashboard → Webhooks. This value is encrypted and cannot be viewed after saving.
                </p>
            </div>

            <button type="submit" 
                    class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                <i class="fas fa-save mr-2"></i>
                Save Webhook Secret
            </button>
        </form>
    </div>

    <!-- Instructions -->
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h4 class="text-sm font-medium text-blue-900 mb-3">
            <i class="fas fa-book mr-2"></i>Setup Instructions
        </h4>
        <div class="text-sm text-blue-800 space-y-2">
            <p><strong>1. Get your Stripe API keys:</strong></p>
            <ul class="list-disc list-inside ml-4 space-y-1">
                <li>Log in to your <a href="https://dashboard.stripe.com" target="_blank" class="underline">Stripe Dashboard</a></li>
                <li>Go to Developers → API keys</li>
                <li>Copy your Publishable key and Secret key</li>
            </ul>
            
            <p class="mt-3"><strong>2. Set up webhooks:</strong></p>
            <ul class="list-disc list-inside ml-4 space-y-1">
                <li>Go to Developers → Webhooks</li>
                <li>Click "Add endpoint"</li>
                <li>Enter your webhook URL: <code class="bg-white px-2 py-1 rounded"><?= htmlspecialchars($_SERVER['HTTP_HOST']) ?>/api/webhooks/stripe</code></li>
                <li>Select events: <code>checkout.session.completed</code>, <code>payment_intent.succeeded</code>, <code>charge.refunded</code>, <code>charge.dispute.created</code></li>
                <li>Copy the Signing secret</li>
            </ul>
            
            <p class="mt-3"><strong>3. Testing:</strong></p>
            <ul class="list-disc list-inside ml-4 space-y-1">
                <li>Use test keys (starting with <code>sk_test_</code> and <code>pk_test_</code>) for development</li>
                <li>Use test card: <code>4242 4242 4242 4242</code> with any future expiry and CVC</li>
                <li>Switch to live keys when ready for production</li>
            </ul>
        </div>
    </div>

    <!-- Current Status -->
    <div class="bg-white rounded-lg shadow p-6">
        <h4 class="text-sm font-medium text-gray-900 mb-3">Configuration Status</h4>
        <div class="space-y-2">
            <div class="flex items-center justify-between py-2">
                <span class="text-sm text-gray-700">Secret Key</span>
                <?php if (isset($settings['stripe_secret_key'])): ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        <i class="fas fa-check-circle mr-1"></i>Configured
                    </span>
                <?php else: ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        <i class="fas fa-times-circle mr-1"></i>Not Set
                    </span>
                <?php endif; ?>
            </div>
            
            <div class="flex items-center justify-between py-2">
                <span class="text-sm text-gray-700">Publishable Key</span>
                <?php if (isset($settings['stripe_publishable_key'])): ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        <i class="fas fa-check-circle mr-1"></i>Configured
                    </span>
                <?php else: ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        <i class="fas fa-times-circle mr-1"></i>Not Set
                    </span>
                <?php endif; ?>
            </div>
            
            <div class="flex items-center justify-between py-2">
                <span class="text-sm text-gray-700">Webhook Secret</span>
                <?php if (isset($settings['stripe_webhook_secret'])): ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        <i class="fas fa-check-circle mr-1"></i>Configured
                    </span>
                <?php else: ?>
                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        <i class="fas fa-times-circle mr-1"></i>Not Set
                    </span>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
