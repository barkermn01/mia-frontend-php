<div class="space-y-6">
    <!-- Success Message -->
    <?php if (isset($_GET['success'])): ?>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-check-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($_GET['success']) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Header -->
    <div class="flex items-center justify-between">
        <h3 class="text-lg font-semibold text-gray-900">Products Management</h3>
        <a href="<?= $adminPath ?>/products/add" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
            <i class="fas fa-plus mr-2"></i>Add Product
        </a>
    </div>

    <!-- Search and Filters -->
    <div class="bg-white rounded-lg shadow p-6">
        <form method="GET" class="flex items-center space-x-4">
            <div class="flex-1">
                <input type="text" name="search" value="<?= htmlspecialchars($search) ?>" 
                       placeholder="Search products..." 
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <button type="submit" class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 transition-colors">
                <i class="fas fa-search mr-2"></i>Search
            </button>
            <?php if ($search): ?>
                <a href="?" class="bg-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-400 transition-colors">
                    <i class="fas fa-times mr-2"></i>Clear
                </a>
            <?php endif; ?>
        </form>
    </div>

    <!-- Products Table -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <?php if (!empty($products)): ?>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Product</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">SKU</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Price</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <?php foreach ($products as $product): ?>
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 h-12 w-12">
                                            <?php if (!empty($product['images'])): ?>
                                                <img class="h-12 w-12 rounded-lg object-cover" 
                                                     src="<?= is_array($product['images'][0])? $product['images'][0]['url']:$product['images'][0]  ?>" 
                                                     alt="<?= htmlspecialchars($product['title']) ?>">
                                            <?php else: ?>
                                                <div class="h-12 w-12 bg-gray-200 rounded-lg flex items-center justify-center">
                                                    <i class="fas fa-image text-gray-400"></i>
                                                </div>
                                            <?php endif; ?>
                                        </div>
                                        <div class="ml-4">
                                            <div class="text-sm font-medium text-gray-900">
                                                <?= htmlspecialchars($product['title']) ?>
                                            </div>
                                            <div class="text-sm text-gray-500">
                                                ID: <?= htmlspecialchars($product['guid'] ?? $product['id']) ?>
                                            </div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                    <?php if (!empty($product['variants'])): ?>
                                        <?= htmlspecialchars($product['variants'][0]['sku']) ?>
                                        <?php if (count($product['variants']) > 1): ?>
                                            <span class="text-gray-500">(+<?= count($product['variants']) - 1 ?> more)</span>
                                        <?php endif; ?>
                                    <?php else: ?>
                                        <span class="text-orange-500">API Issue: Variants not returned</span>
                                    <?php endif; ?>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                    <?php if (!empty($product['variants'])): ?>
                                        <?php if (count($product['variants']) === 1): ?>
                                            <!-- Single variant - show its price -->
                                            <?= $view->formatPrice($product['variants'][0]['price']) ?>
                                        <?php else: ?>
                                            <!-- Multiple variants - show "Multiple" with tooltip -->
                                            <span class="text-blue-600 cursor-help border-b border-dotted border-blue-600" 
                                                  data-tooltip="#price-tooltip-<?= $product['id'] ?>">
                                                Multiple
                                            </span>
                                            <!-- Hidden tooltip content -->
                                            <div id="price-tooltip-<?= $product['id'] ?>" class="tooltip-content">
                                                <div class="bg-gray-800 text-white text-xs rounded py-2 px-3 max-w-xs">
                                                    <div class="font-semibold mb-2">Variant Prices:</div>
                                                    <?php 
                                                    $displayVariants = array_slice($product['variants'], 0, 10);
                                                    foreach ($displayVariants as $variant): ?>
                                                        <div class="flex justify-between mb-1">
                                                            <span class="truncate mr-2"><?= htmlspecialchars($variant['sku']) ?></span>
                                                            <span><?= $view->formatPrice($variant['price']) ?></span>
                                                        </div>
                                                    <?php endforeach; ?>
                                                    <?php if (count($product['variants']) > 10): ?>
                                                        <div class="text-gray-300 text-center mt-2 pt-2 border-t border-gray-600">
                                                            (<?= count($product['variants']) - 10 ?> more)
                                                        </div>
                                                    <?php endif; ?>
                                                </div>
                                            </div>
                                        <?php endif; ?>
                                    <?php elseif (!empty($product['price'])): ?>
                                        <!-- Legacy price format -->
                                        <?= $view->formatPrice($product['price']) ?>
                                    <?php else: ?>
                                        <span class="text-gray-400">No price</span>
                                    <?php endif; ?>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">
                                        Active
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                    <div class="flex space-x-2">
                                        <?php
                                        // Generate SEO-friendly product URL for "View on Site"
                                        $productId = $product['guid'] ?? $product['id'];
                                        $productTitle = $product['title'] ?? 'Product';
                                        $productSlug = strtolower(trim($productTitle));
                                        $productSlug = preg_replace('/[^a-z0-9]+/', '-', $productSlug);
                                        $productSlug = trim($productSlug, '-');
                                        if (strlen($productSlug) > 50) {
                                            $productSlug = substr($productSlug, 0, 50);
                                            $productSlug = rtrim($productSlug, '-');
                                        }
                                        $productSlug = $productSlug ?: 'product';
                                        $seoProductUrl = "/product/{$productId}/{$productSlug}";
                                        ?>
                                        <a href="<?= $seoProductUrl ?>" 
                                           target="_blank" 
                                           class="text-green-600 hover:text-green-900 transition-colors" 
                                           title="View on Site">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <button class="manage-stock-btn text-purple-600 hover:text-purple-900 transition-colors" 
                                                title="Manage Stock" 
                                                data-product-id="<?= htmlspecialchars($product['guid'] ?? $product['id']) ?>">
                                            <i class="fas fa-warehouse"></i>
                                        </button>
                                        <a href="<?= $adminPath ?>/products/edit?id=<?= urlencode($product['guid'] ?? $product['id']) ?>" 
                                           class="text-blue-600 hover:text-blue-900 transition-colors" title="Edit Product">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <button class="delete-product-btn text-red-600 hover:text-red-900 transition-colors" 
                                                title="Delete Product" 
                                                data-product-id="<?= htmlspecialchars($product['guid'] ?? $product['id']) ?>">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <?php if ($total > 20): ?>
                <div class="bg-white px-4 py-3 border-t border-gray-200 sm:px-6">
                    <div class="flex items-center justify-between">
                        <div class="text-sm text-gray-700">
                            Showing <?= (($page - 1) * 20) + 1 ?> to <?= min($page * 20, $total) ?> of <?= $total ?> results
                        </div>
                        <div class="flex space-x-2">
                            <?php if ($page > 1): ?>
                                <a href="?page=<?= $page - 1 ?><?= $search ? '&search=' . urlencode($search) : '' ?>" 
                                   class="px-3 py-2 text-sm bg-gray-300 text-gray-700 rounded hover:bg-gray-400">
                                    Previous
                                </a>
                            <?php endif; ?>
                            <?php if ($page * 20 < $total): ?>
                                <a href="?page=<?= $page + 1 ?><?= $search ? '&search=' . urlencode($search) : '' ?>" 
                                   class="px-3 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700">
                                    Next
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            <?php endif; ?>
        <?php else: ?>
            <div class="text-center py-12">
                <i class="fas fa-box text-4xl text-gray-400 mb-4"></i>
                <h3 class="text-lg font-medium text-gray-900 mb-2">No products found</h3>
                <p class="text-gray-500 mb-4">
                    <?= $search ? 'No products match your search criteria.' : 'Get started by adding your first product.' ?>
                </p>
                <a href="<?= $adminPath ?>/products/add" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                    <i class="fas fa-plus mr-2"></i>Add Product
                </a>
            </div>
        <?php endif; ?>
    </div>
</div>

<script src="/js/ModalManager.js"></script>
<script src="/js/admin/products.js"></script>
<script>
// Initialize the admin products with configuration data
document.addEventListener('DOMContentLoaded', function() {
    window.AdminProductsConfig = {
        adminPath: '<?= $adminPath ?>'
    };
});
</script>