<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <div>
            <h3 class="text-lg font-semibold text-gray-900">Payment Alerts</h3>
            <p class="text-sm text-gray-600 mt-1">Monitor payment issues and chargebacks</p>
        </div>
        <div class="flex items-center space-x-3">
            <!-- Filter by Severity -->
            <select id="severity-filter" onchange="filterAlerts(this.value)" 
                    class="px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <option value="">All Severities</option>
                <option value="critical" <?= ($severity ?? '') === 'critical' ? 'selected' : '' ?>>Critical</option>
                <option value="warning" <?= ($severity ?? '') === 'warning' ? 'selected' : '' ?>>Warning</option>
                <option value="info" <?= ($severity ?? '') === 'info' ? 'selected' : '' ?>>Info</option>
            </select>
            
            <?php if ($unreadCount > 0): ?>
                <button onclick="markAllAsRead()" 
                        class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                    <i class="fas fa-check-double mr-2"></i>
                    Mark All as Read
                </button>
            <?php endif; ?>
        </div>
    </div>

    <!-- Alert Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
        <div class="bg-white rounded-lg shadow p-4">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-gray-600">Total Alerts</p>
                    <p class="text-2xl font-bold text-gray-900"><?= $total ?></p>
                </div>
                <div class="bg-blue-100 rounded-full p-3">
                    <i class="fas fa-bell text-blue-600 text-xl"></i>
                </div>
            </div>
        </div>
        
        <div class="bg-white rounded-lg shadow p-4">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-gray-600">Unread</p>
                    <p class="text-2xl font-bold text-red-600"><?= $unreadCount ?></p>
                </div>
                <div class="bg-red-100 rounded-full p-3">
                    <i class="fas fa-exclamation-circle text-red-600 text-xl"></i>
                </div>
            </div>
        </div>
        
        <div class="bg-white rounded-lg shadow p-4">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-gray-600">Critical</p>
                    <p class="text-2xl font-bold text-orange-600"><?= $criticalCount ?></p>
                </div>
                <div class="bg-orange-100 rounded-full p-3">
                    <i class="fas fa-exclamation-triangle text-orange-600 text-xl"></i>
                </div>
            </div>
        </div>
        
        <div class="bg-white rounded-lg shadow p-4">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-gray-600">Today</p>
                    <p class="text-2xl font-bold text-gray-900"><?= $todayCount ?></p>
                </div>
                <div class="bg-gray-100 rounded-full p-3">
                    <i class="fas fa-calendar-day text-gray-600 text-xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Alerts List -->
    <?php if (!empty($alerts)): ?>
        <div class="space-y-4">
            <?php foreach ($alerts as $alert): ?>
                <div class="bg-white rounded-lg shadow overflow-hidden <?= !$alert['read'] ? 'border-l-4 border-red-500' : '' ?>">
                    <div class="p-6">
                        <div class="flex items-start justify-between">
                            <div class="flex-1">
                                <!-- Alert Header -->
                                <div class="flex items-center space-x-3 mb-2">
                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold
                                        <?php 
                                        switch($alert['severity']) {
                                            case 'critical': echo 'bg-red-100 text-red-800'; break;
                                            case 'warning': echo 'bg-yellow-100 text-yellow-800'; break;
                                            case 'info': echo 'bg-blue-100 text-blue-800'; break;
                                            default: echo 'bg-gray-100 text-gray-800';
                                        }
                                        ?>">
                                        <?= ucfirst(htmlspecialchars($alert['severity'])) ?>
                                    </span>
                                    
                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-purple-100 text-purple-800">
                                        <?= htmlspecialchars($alert['type']) ?>
                                    </span>
                                    
                                    <?php if (!$alert['read']): ?>
                                        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-red-100 text-red-800">
                                            <i class="fas fa-circle text-xs mr-1"></i>
                                            Unread
                                        </span>
                                    <?php endif; ?>
                                </div>
                                
                                <!-- Alert Message -->
                                <h4 class="text-lg font-semibold text-gray-900 mb-2">
                                    <?= htmlspecialchars($alert['message']) ?>
                                </h4>
                                
                                <!-- Alert Details -->
                                <div class="grid grid-cols-2 gap-4 text-sm text-gray-600 mb-3">
                                    <div>
                                        <span class="font-medium">Order:</span>
                                        <a href="<?= $adminPath ?>/orders/<?= htmlspecialchars($alert['orderId']) ?>" 
                                           class="text-blue-600 hover:text-blue-800 ml-1">
                                            #<?= htmlspecialchars($alert['orderNumber'] ?? $alert['orderId']) ?>
                                        </a>
                                    </div>
                                    <div>
                                        <span class="font-medium">Date:</span>
                                        <span class="ml-1">
                                            <?php 
                                            $alertCreatedAt = $alert['createdAt'];
                                            if (is_numeric($alertCreatedAt)) {
                                                echo date('F j, Y g:i A', (int)$alertCreatedAt);
                                            } elseif (is_string($alertCreatedAt)) {
                                                echo date('F j, Y g:i A', strtotime($alertCreatedAt));
                                            } else {
                                                echo 'N/A';
                                            }
                                            ?>
                                        </span>
                                    </div>
                                </div>
                                
                                <!-- Additional Data -->
                                <?php if (!empty($alert['data'])): ?>
                                    <div class="bg-gray-50 rounded-lg p-3 text-sm">
                                        <p class="font-medium text-gray-700 mb-2">Additional Information:</p>
                                        <div class="space-y-1 text-gray-600">
                                            <?php foreach ($alert['data'] as $key => $value): ?>
                                                <div class="flex">
                                                    <span class="font-medium w-32"><?= htmlspecialchars(ucwords(str_replace('_', ' ', $key))) ?>:</span>
                                                    <span><?= htmlspecialchars(is_array($value) ? json_encode($value) : $value) ?></span>
                                                </div>
                                            <?php endforeach; ?>
                                        </div>
                                    </div>
                                <?php endif; ?>
                            </div>
                            
                            <!-- Actions -->
                            <div class="ml-4 flex flex-col space-y-2">
                                <?php if (!$alert['read']): ?>
                                    <button onclick="markAlertAsRead('<?= htmlspecialchars($alert['orderId']) ?>', '<?= htmlspecialchars($alert['alertId']) ?>')"
                                            class="bg-blue-600 text-white px-3 py-2 rounded-lg hover:bg-blue-700 transition-colors text-sm">
                                        <i class="fas fa-check mr-1"></i>
                                        Mark as Read
                                    </button>
                                <?php endif; ?>
                                
                                <a href="<?= $adminPath ?>/orders/<?= htmlspecialchars($alert['orderId']) ?>" 
                                   class="bg-gray-100 text-gray-700 px-3 py-2 rounded-lg hover:bg-gray-200 transition-colors text-sm text-center">
                                    <i class="fas fa-eye mr-1"></i>
                                    View Order
                                </a>
                                
                                <button onclick="deleteAlert('<?= htmlspecialchars($alert['orderId']) ?>', '<?= htmlspecialchars($alert['alertId']) ?>')"
                                        class="bg-red-100 text-red-700 px-3 py-2 rounded-lg hover:bg-red-200 transition-colors text-sm">
                                    <i class="fas fa-trash mr-1"></i>
                                    Delete
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
        
        <!-- Pagination -->
        <?php if ($total > 20): ?>
            <div class="bg-white rounded-lg shadow px-4 py-3">
                <div class="flex items-center justify-between">
                    <div class="text-sm text-gray-700">
                        Showing <?= (($page - 1) * 20) + 1 ?> to <?= min($page * 20, $total) ?> of <?= $total ?> alerts
                    </div>
                    <div class="flex space-x-2">
                        <?php if ($page > 1): ?>
                            <a href="?page=<?= $page - 1 ?><?= $severity ? '&severity=' . $severity : '' ?>" 
                               class="px-3 py-2 text-sm bg-gray-300 text-gray-700 rounded hover:bg-gray-400">
                                Previous
                            </a>
                        <?php endif; ?>
                        <?php if ($page * 20 < $total): ?>
                            <a href="?page=<?= $page + 1 ?><?= $severity ? '&severity=' . $severity : '' ?>" 
                               class="px-3 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-700">
                                Next
                            </a>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        <?php endif; ?>
        
    <?php else: ?>
        <!-- Empty State -->
        <div class="bg-white rounded-lg shadow p-12 text-center">
            <i class="fas fa-bell-slash text-4xl text-gray-400 mb-4"></i>
            <h3 class="text-lg font-medium text-gray-900 mb-2">No Alerts</h3>
            <p class="text-gray-500">
                <?= $severity ? 'No alerts found with the selected severity.' : 'All clear! No payment alerts at this time.' ?>
            </p>
        </div>
    <?php endif; ?>
</div>

<script>
function filterAlerts(severity) {
    const url = new URL(window.location.href);
    if (severity) {
        url.searchParams.set('severity', severity);
    } else {
        url.searchParams.delete('severity');
    }
    url.searchParams.delete('page'); // Reset to page 1
    window.location.href = url.toString();
}

async function markAlertAsRead(orderId, alertId) {
    try {
        const response = await fetch(`<?= $adminPath ?>/api/alerts/${orderId}/${alertId}/read`, {
            method: 'PATCH'
        });
        
        if (response.ok) {
            window.location.reload();
        } else {
            alert('Failed to mark alert as read');
        }
    } catch (error) {
        console.error('Error:', error);
        alert('Failed to mark alert as read');
    }
}

async function markAllAsRead() {
    if (!confirm('Mark all alerts as read?')) return;
    
    try {
        const response = await fetch(`<?= $adminPath ?>/api/alerts/read-all`, {
            method: 'PATCH'
        });
        
        if (response.ok) {
            window.location.reload();
        } else {
            alert('Failed to mark all alerts as read');
        }
    } catch (error) {
        console.error('Error:', error);
        alert('Failed to mark all alerts as read');
    }
}

async function deleteAlert(orderId, alertId) {
    if (!confirm('Delete this alert?')) return;
    
    try {
        const response = await fetch(`<?= $adminPath ?>/api/alerts/${orderId}/${alertId}`, {
            method: 'DELETE'
        });
        
        if (response.ok) {
            window.location.reload();
        } else {
            alert('Failed to delete alert');
        }
    } catch (error) {
        console.error('Error:', error);
        alert('Failed to delete alert');
    }
}
</script>
