<div class="space-y-6">
    <!-- Success Message -->
    <?php if (isset($_GET['success'])): ?>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
            <div class="flex">
                <i class="fas fa-check-circle mr-2 mt-0.5"></i>
                <span><?= htmlspecialchars($_GET['success']) ?></span>
            </div>
        </div>
    <?php endif; ?>

    <!-- Header -->
    <div class="flex items-center justify-between">
        <div>
            <h3 class="text-lg font-semibold text-gray-900">Shipping Methods</h3>
            <p class="text-sm text-gray-600 mt-1">Manage shipping methods and regional pricing</p>
        </div>
        <a href="<?= $adminPath ?>/shipping/add" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
            <i class="fas fa-plus mr-2"></i>Add Shipping Method
        </a>
    </div>

    <!-- Shipping Calculation Mode Toggle (Compact) -->
    <div class="bg-white rounded-lg shadow">
        <button type="button" id="toggle-mode-section" class="w-full px-6 py-4 flex items-center justify-between hover:bg-gray-50 transition-colors">
            <div class="flex items-center">
                <i class="fas fa-calculator text-gray-600 mr-3"></i>
                <div class="text-left">
                    <div class="font-medium text-gray-900">Shipping Calculation Mode</div>
                    <div class="text-sm text-gray-600">
                        Current: <span id="current-mode-display-compact" class="font-medium">Loading...</span>
                    </div>
                </div>
            </div>
            <i class="fas fa-chevron-down text-gray-400 transition-transform" id="mode-section-icon"></i>
        </button>
        
        <div id="mode-section-content" class="hidden border-t border-gray-200 px-6 py-4">
            <div class="space-y-3 mb-4">
                <label class="flex items-start p-3 border-2 rounded-lg cursor-pointer hover:bg-gray-50 transition-colors" id="mode-modern-label">
                    <input type="radio" name="shipping_mode" value="modern" id="mode-modern" class="mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500">
                    <div class="ml-3">
                        <div class="font-medium text-gray-900">Modern Mode</div>
                        <div class="text-sm text-gray-600">Base cost + cost per kilogram (e.g., £5.00 + £2.00/kg)</div>
                    </div>
                </label>
                
                <label class="flex items-start p-3 border-2 rounded-lg cursor-pointer hover:bg-gray-50 transition-colors" id="mode-classic-label">
                    <input type="radio" name="shipping_mode" value="classic" id="mode-classic" class="mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500">
                    <div class="ml-3">
                        <div class="font-medium text-gray-900">Classic Mode</div>
                        <div class="text-sm text-gray-600">Weight bracket table (e.g., 0-1kg: £5.00, 1-2kg: £7.50)</div>
                    </div>
                </label>
            </div>
            
            <div class="flex items-center justify-between pt-3 border-t border-gray-200">
                <div class="text-xs text-amber-600">
                    <i class="fas fa-exclamation-triangle mr-1"></i>
                    Changing modes requires reconfiguring regions
                </div>
                <button type="button" id="save-mode-btn" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors disabled:bg-gray-400 disabled:cursor-not-allowed text-sm" disabled>
                    <i class="fas fa-save mr-2"></i>Save Mode
                </button>
            </div>
        </div>
    </div>

    <!-- Shipping Methods List -->
    <?php if (!empty($methods)): ?>
        <div class="grid gap-6">
            <?php foreach ($methods as $method): ?>
                <div class="bg-white rounded-lg shadow overflow-hidden">
                    <div class="p-6">
                        <div class="flex items-start justify-between">
                            <div class="flex-1">
                                <div class="flex items-center space-x-3 mb-2">
                                    <h4 class="text-lg font-semibold text-gray-900"><?= htmlspecialchars($method['name']) ?></h4>
                                    <?php if ($method['enabled']): ?>
                                        <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">
                                            Enabled
                                        </span>
                                    <?php else: ?>
                                        <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-gray-100 text-gray-800">
                                            Disabled
                                        </span>
                                    <?php endif; ?>
                                </div>
                                
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-4">
                                    <?php if (!empty($method['provider'])): ?>
                                        <div>
                                            <span class="text-sm text-gray-500">Provider</span>
                                            <p class="text-sm font-medium text-gray-900"><?= htmlspecialchars($method['provider']) ?></p>
                                        </div>
                                    <?php endif; ?>
                                    
                                    <?php if (!empty($method['deliveryTime'])): ?>
                                        <div>
                                            <span class="text-sm text-gray-500">Delivery Time</span>
                                            <p class="text-sm font-medium text-gray-900"><?= htmlspecialchars($method['deliveryTime']) ?></p>
                                        </div>
                                    <?php endif; ?>
                                    
                                    <?php if (!empty($method['maxWeightKg'])): ?>
                                        <div>
                                            <span class="text-sm text-gray-500">Max Weight</span>
                                            <p class="text-sm font-medium text-gray-900"><?= number_format($method['maxWeightKg'], 2) ?> kg</p>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <!-- Regions -->
                                <?php if (!empty($method['regions'])): ?>
                                    <div class="mt-4">
                                        <button type="button" 
                                                class="toggle-regions-btn flex items-center justify-between w-full text-left text-sm font-medium text-gray-700 hover:text-gray-900 focus:outline-none"
                                                data-method-id="<?= htmlspecialchars($method['id']) ?>">
                                            <span>Regions (<?= count($method['regions']) ?>)</span>
                                            <i class="fas fa-chevron-down transition-transform"></i>
                                        </button>
                                        <div class="regions-content hidden mt-3">
                                            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
                                                <?php foreach ($method['regions'] as $region): ?>
                                                    <div class="border border-gray-200 rounded-lg p-3">
                                                        <div class="flex items-center justify-between mb-2">
                                                            <span class="font-medium text-gray-900"><?= htmlspecialchars($region['countryCode']) ?></span>
                                                            <?php if (!empty($region['countryName'])): ?>
                                                                <span class="text-xs text-gray-500"><?= htmlspecialchars($region['countryName']) ?></span>
                                                            <?php endif; ?>
                                                        </div>
                                                        <div class="space-y-1 text-xs text-gray-600">
                                                            <?php if (!empty($region['weightBrackets'])): ?>
                                                                <!-- Classic Mode: Weight Brackets -->
                                                                <div class="font-medium text-gray-700">Weight Brackets:</div>
                                                                <?php foreach (array_slice($region['weightBrackets'], 0, 3) as $bracket): ?>
                                                                    <div><?= number_format($bracket['minKg'], 1) ?>-<?= number_format($bracket['maxKg'], 1) ?>kg: <?= $view->formatPrice($bracket['cost']) ?></div>
                                                                <?php endforeach; ?>
                                                                <?php if (count($region['weightBrackets']) > 3): ?>
                                                                    <div class="text-gray-500">+<?= count($region['weightBrackets']) - 3 ?> more...</div>
                                                                <?php endif; ?>
                                                            <?php else: ?>
                                                                <!-- Modern Mode: Base + Per Kg -->
                                                                <div>Base: <?= $view->formatPrice($region['baseCost']) ?></div>
                                                                <?php if (!empty($region['costPerKg'])): ?>
                                                                    <div>Per kg: <?= $view->formatPrice($region['costPerKg']) ?></div>
                                                                <?php endif; ?>
                                                            <?php endif; ?>
                                                            <?php if (!empty($region['freeShippingThreshold'])): ?>
                                                                <div class="text-green-600">Free over: <?= $view->formatPrice($region['freeShippingThreshold']) ?></div>
                                                            <?php endif; ?>
                                                        </div>
                                                    </div>
                                                <?php endforeach; ?>
                                            </div>
                                        </div>
                                    </div>
                                <?php endif; ?>
                            </div>

                            <div class="flex space-x-3 ml-4">
                                <a href="<?= $adminPath ?>/shipping/edit?id=<?= urlencode($method['id']) ?>" 
                                   class="text-blue-600 hover:text-blue-900 transition-colors" title="Edit">
                                    <i class="fas fa-edit fa-lg"></i>
                                </a>
                                <button class="delete-shipping-btn text-red-600 hover:text-red-900 transition-colors" 
                                        title="Delete" 
                                        data-method-id="<?= htmlspecialchars($method['id']) ?>"
                                        data-method-name="<?= htmlspecialchars($method['name']) ?>">
                                    <i class="fas fa-trash fa-lg"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <div class="bg-white rounded-lg shadow p-12 text-center">
            <i class="fas fa-shipping-fast text-4xl text-gray-400 mb-4"></i>
            <h3 class="text-lg font-medium text-gray-900 mb-2">No shipping methods configured</h3>
            <p class="text-gray-500 mb-4">Get started by adding your first shipping method</p>
            <a href="<?= $adminPath ?>/shipping/add" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors inline-block">
                <i class="fas fa-plus mr-2"></i>Add Shipping Method
            </a>
        </div>
    <?php endif; ?>
</div>

<script src="/js/admin/shipping.js"></script>
